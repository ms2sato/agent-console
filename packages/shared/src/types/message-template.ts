/**
 * A saved message template for quick insertion into the message input.
 */
export interface MessageTemplate {
  id: string;
  title: string;
  content: string;
  sortOrder: number;
  createdAt: string;
  updatedAt: string;
}
