/**
 * Integration types for external service connections.
 * Used for both outbound (notifications) and potentially inbound (webhooks) integrations.
 */

/** Integration service types */
export type IntegrationType = 'slack';
