/**
 * Skill definition extracted from SKILL.md frontmatter.
 * Name is formatted as `/<skill-name>` (e.g., `/commit`, `/orchestrator`).
 */
export interface SkillDefinition {
  name: string;
  description: string;
}
