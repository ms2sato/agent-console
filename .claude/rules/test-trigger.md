---
globs:
  - "packages/server/src/routes/**/*.ts"
  - "packages/server/src/services/**/*.ts"
  - "packages/server/src/mcp/**/*.ts"
  - "packages/server/src/lib/**/*.ts"
  - "packages/server/src/repositories/**/*.ts"
  - "packages/client/src/hooks/**/*.ts"
  - "packages/client/src/components/**/*.tsx"
  - "packages/shared/src/**/*.ts"
  - "packages/embedded-agent/src/**/*.ts"
  - ".claude/hooks/**/*.sh"
  - "scripts/lib/**/*.ts"
  - "!**/*.test.ts"
  - "!**/*.test.tsx"
  - "!**/*.test.mjs"
  - "!**/__tests__/**"
---

# Test Coverage Requirement

When modifying production files matching these patterns, corresponding test files **must** be added or updated.

## Expected Test File Locations

| File Pattern | Expected Test Location |
|-------------|------------------------|
| `packages/server/src/routes/**/*.ts` | `.../__tests__/*.test.ts` or sibling `*.test.ts` |
| `packages/server/src/services/**/*.ts` | `.../__tests__/*.test.ts` or sibling `*.test.ts` |
| `packages/server/src/mcp/**/*.ts` | `.../__tests__/*.test.ts` or sibling `*.test.ts` |
| `packages/server/src/lib/**/*.ts` | `.../__tests__/*.test.ts` or sibling `*.test.ts` |
| `packages/server/src/repositories/**/*.ts` | `.../__tests__/*.test.ts` or sibling `*.test.ts` |
| `packages/client/src/hooks/**/*.ts` | `.../__tests__/*.test.ts(x)` or sibling `*.test.ts(x)` |
| `packages/client/src/components/**/*.tsx` | `.../__tests__/*.test.tsx` or sibling `*.test.tsx` (a JSX-free pure-logic test may instead use `*.test.ts`, e.g. `SessionPage.test.ts` alongside `SessionPage.tsx`) |
| `packages/shared/src/**/*.ts` | `.../__tests__/*.test.ts` or sibling `*.test.ts` |
| `packages/embedded-agent/src/**/*.ts` | `.../__tests__/*.test.ts` or sibling `*.test.ts` |
| `.claude/hooks/**/*.sh` | `.claude/hooks/__tests__/*.test.mjs` or sibling `*.test.mjs` |
| `scripts/lib/**/*.ts` | `.../__tests__/*.test.ts` or sibling `*.test.ts` (the bun entries the shell lib shells out to, e.g. `embedded-agent-bun-identity.ts`; the `.sh` lib itself is covered by `scripts/__tests__/setup-multiuser-checks.test.mjs` via its subcommand dispatcher) |

## Exceptions

- **`packages/integration/src/`** uses a flat sibling layout (no `__tests__/` directory) for its boundary tests. This is deliberate: the package contains no production code — its entire `src/` is test infrastructure (`setup.ts`, `test-utils.ts`) and boundary tests (`*-boundary.test.ts(x)`). Do not move these files into a `__tests__/` subdirectory. The one exception is `src/e2e-native/`, which holds the two shipping-path e2e test files that require a separate `bun test` invocation without the happy-dom preload (see `src/e2e-native/setup-native.ts` for why, and `package.json`'s `test` / `test:coverage` / `test:watch` scripts for the two-invocation chain). This subdirectory is process-partitioning infrastructure, not a `__tests__/`-shaped test-to-production mapping, so it does not contradict the flat-layout rationale above.
- **`packages/server`'s five real-fs test files** (`lib/__tests__/memory-dir.test.ts`, `lib/__tests__/trusted-dir.test.ts`, `routes/__tests__/workers-upload-dir-real-fs.test.ts`, `__tests__/utils/__tests__/scratch-git.test.ts`, `services/__tests__/git-diff-base-reresolve.test.ts`) stay in their ordinary sibling `__tests__/` location — no subdirectory, no filename suffix — because `packages/server` has no `bunfig.toml` / preload to switch off; memfs becomes process-global because `mock-fs-helper.ts`'s `mock.module('fs'/'fs/promises', ...)` is irreversible for the life of whichever `bun:test` PROCESS loads it first (see this file's memory-layer smoke section above and `packages/server/src/__tests__/utils/memfs-detection.ts`'s header). The first three assert mode bits / gid inheritance / symlink semantics directly; the latter two hand a real subprocess -- `git` -- a directory memfs cannot make visible to it, the same underlying class (something outside the JS-level fs simulation) reached from a different angle. The fix is a SECOND `bun test` PROCESS, selected by an explicit path list in `packages/server/package.json`'s `test` / `test:coverage` / `test:watch` + `test:watch:real-fs` scripts (moving the files to a subdir would break `check-utils.js`'s sibling-test mapping for `lib/memory-dir.ts` / `lib/trusted-dir.ts` / `routes/workers.ts`). Each real-fs case calls `assertRealFs(label)` (in `memfs-detection.ts`, beside `isMemfsActive`) instead of silently skipping under memfs; the list is kept honest in both directions by `packages/server/src/__tests__/real-fs-invocation.test.ts` (Issue [#1699](https://github.com/ms2sato/agent-console/issues/1699)).
- **`*.gen.ts` / `*.gen.tsx`** files are build-time generated (e.g. `packages/shared/src/schema-version.gen.ts` emitted by a codegen step). Their contents derive from an authoritative source at build time, so a hand-written sibling test would be tautological — test the generator, not its emitted output. The exclusion is anchored on the `.gen.<ext>$` suffix; files like `generator.ts` (substring "gen", no `.gen.` suffix) still require coverage.
- **Bare `types.ts` / `types.tsx` as a full path segment** are module-level type-definitions files colocated with their consumers (a natural React / Node.js convention — e.g. `packages/embedded-agent/src/tools/types.ts`). Same rationale as the `-types.ts` convention above: the type system enforces shape at consume sites. Exclusion is anchored on the segment boundary, so files like `mytypes.ts` (mid-segment match) or `type.ts` (singular, may contain runtime enums / factories) still require coverage.
- **`<x>-repository.ts` under `packages/server/src/repositories/`** whose basename does NOT start with `sqlite-` / `json-` / `inbound-` are interface-only contract files (`export interface` / `export type` only, no runtime logic). Same rationale as the `-types.ts` convention above — the type system already enforces their shape at consume sites, so a runtime test would be tautological. Implementations whose basename starts with those prefixes (`sqlite-*-repository.ts`, `json-*-repository.ts`, `inbound-*-repository.ts`) and `repository-factory.ts` (runtime code, not a contract file) remain in scope. `index.ts` in that directory is covered by the existing re-export-only content exemption above, not by a path regex.
- **Comment-only diffs** are exempted content-based, not path-based (Issue #1189), and the exemption applies to **both** checks that gate on a file's diff content: `check-utils.js`'s `findTestFiles()` (sibling-test coverage) and `detectIntegrationTestNeeds()` (integration-test-gap detection, extended to this exemption by Issue [#1816](https://github.com/ms2sato/agent-console/issues/1816) — before that, a comment-only diff to a `packages/shared/src/**` / route / component path still tripped the "raise it with the requester" flag). Both call the same underlying `isCommentOnlyFileDiff()` (which wraps `isCommentOnlyDiff()`) to inspect a file's actual diff hunks against the base branch (`git diff --unified=0`); if every added/removed line is a comment (`//`, `/* */` block, or `#` for `.sh`) or blank, the corresponding requirement — sibling test, or integration-test-gap trigger — is skipped for that file. A mixed diff (any real code line alongside comment changes) still requires a sibling test and still trips the integration-test-gap flag. This exception cannot be expressed as a glob and is intentionally excluded from `check-mirror-drift.js`'s comparison, same as the `isTestFile()` negation entries above.

  Both `preflight-check.js` and `acceptance-check.js` call the same `findTestFiles()` and `detectIntegrationTestNeeds()`, so this exemption has always been a single writer, not duplicated logic — Issue [#1463](https://github.com/ms2sato/agent-console/issues/1463) was filed on the mistaken premise that it wasn't. What was actually broken: the diff `findTestFiles()` (and, since #1816, `detectIntegrationTestNeeds()`) reads defaults to the checked-out worktree's `HEAD`, which is correct when the process is running inside the exact PR being checked (CI's checkout, or `preflight-check.js`'s no-PR-number local mode) but wrong when checking an arbitrary PR number from a *different* worktree — `acceptance-check.js`'s actual usage pattern, run by the Orchestrator against whichever PR they're reviewing. `resolvePrDiffRef(prNumber)` resolves that PR's real base/head SHAs via the GitHub API and fetches them, so the comment-only check diffs against the PR's actual content regardless of what the calling worktree happens to have checked out. It fails loudly (no silent fallback to `HEAD`) if the SHAs can't be resolved or fetched, rather than quietly reproducing the original bug.

## Registering a smoke script (applies to every section below)

**A new `scripts/smoke/*` script gets exactly one way to be registered and one way to be invoked.** Registration is an Additional Verification section here, in the same PR that adds the script — the sections below are instances of that rule, not a closed list; a sixth smoke needs a sixth section, and names its verification tier in one clause (Discipline 4 of `os-environment-coupling.md`). Invocation is `bun scripts/smoke/<file>` — there is no `package.json` `check:` alias for any smoke; two spellings for the same script was the defect Issue [#1637](https://github.com/ms2sato/agent-console/issues/1637) closed.

Do not read "this is a manual gate, never a CI job" as a reason to skip registration. **Every smoke listed below is a manual gate.** Registration is about *reachability* — a script nobody can find is a script nobody re-runs after the change that would have broken it — while automation is about CI, and the two decisions are independent. When a script is billable or needs an authenticated CLI, say so in its section, so a future reader knows the cost before running it rather than after.

(Lesson: Issue [#671](https://github.com/ms2sato/agent-console/issues/671)'s sibling problem, surfaced twice in one day. Two smokes were added in parallel and neither was registered; the second author could not have learned it from the first author's correction, because the requirement existed nowhere as a rule — only as four examples that a reader had to generalise from unprompted. This section is what stops the next one; `scripts/smoke/__tests__/registry-reachability.test.ts` — every `scripts/smoke/*` basename must appear here — is what stops it mechanically.)

**A new smoke script is born import-safe and born-covered.** Write it with a named `main()` whose only top-level invocation is guarded by `if (import.meta.main) { main(); }` — importing an exported function from it (the pattern this file's own sections use for a free, non-billed proxy verification) must never execute the script. `scripts/smoke/__tests__/import-safety.test.ts` discovers every `scripts/smoke/*.{ts,mjs}` via a glob and subprocess-imports each one, so a new script is enforced mechanically with no separate registration step for this specific check (Issue #1479).

**A new smoke script is also born knip-reachable, for the same reason.** `knip.json`'s root workspace `entry` list includes `scripts/smoke/**/*.{ts,mjs}` as a glob, not a per-file list — this is the third mechanical reachability net alongside this file's sections (human-readable registration) and `import-safety.test.ts` (import safety): without it, `bun run lint:unused` (`knip`, CI's `check-structure` job) has no static entry point for a smoke, and a smoke reached only through a removed `package.json` `check:` alias becomes an "unused file" the moment that alias is removed, exactly as happened when Issue #1637 dropped the alias mechanism for `check-artifact-sandbox-boundary.mjs` / `check-artifact-server-story-e2e.mjs` (the only two `.mjs` smokes, `knip.json`'s root `project` scope being `.mjs`-only; `.ts` smokes were invisible to `lint:unused` before this entry pattern existed). A new smoke needs no edit to `knip.json` for this to hold.

## Additional Verification: Preview Sandbox Real-Browser Check

PRs touching `packages/client/src/lib/preview-sandbox.ts`, `packages/client/src/lib/__fixtures__/preview-sandbox-corpus.ts`, or `packages/client/src/components/workers/PreviewPanel.tsx` must run `bun run check:preview-sandbox-browser` locally before pushing. This runs `scripts/run-preview-sandbox-browser-check.mjs`, which re-verifies the mXSS regression corpus against a real Chromium browser — `bun:test`'s happy-dom environment does not reproduce Chromium's HTML5 parsing edge cases (see `.claude/rules/os-environment-coupling.md`). This check is a real-browser regression gate, not a sibling-test requirement, so it is not part of the `preflight-check.js` coverage patterns above. **Verification tier (Discipline 4 of `os-environment-coupling.md`):** Unprivileged (no tier-2/3/4 residue; any machine with bun) — the real-Chromium requirement above is a browser dependency, not an elevation tier.

## Additional Verification: PTY Master FD Leak Check

PRs touching `packages/server/src/lib/pty-provider.ts` or `packages/server/src/services/worker-manager.ts`'s `detachPty` must run `bun scripts/smoke/check-pty-fd-leak.ts` locally before pushing. It drives 100 real spawn/kill cycles through the production `bunTerminalProvider` and asserts that the process's ptmx-fd count (`/proc/self/fd`) and the kernel-wide allocated-pty counter (`/proc/sys/kernel/pty/nr`) stay flat — confirming `BunTerminalPtyAdapter.dispose()` actually releases the `Bun.Terminal` master-fd handle deterministically, rather than relying on the object becoming unreachable and incidentally GC-finalized (unsound in production, where `InternalPtyWorker.pty` stays reachable via session/worker maps for the life of the worker) (see Issue #1196). This check is a real-fd regression gate, not a sibling-test requirement, so it is not part of the `preflight-check.js` coverage patterns above. **Verification tier (Discipline 4 of `os-environment-coupling.md`):** Unprivileged (no tier-2/3/4 residue; any machine with bun).

## Additional Verification: Artifact Sandbox Boundary Real-Browser Check

PRs touching `packages/server/src/routes/artifacts.ts` (the `ARTIFACT_SERVING_CSP` header), `packages/server/src/routes/artifacts-viewer.ts` (the navigation-jail shell), or the `create_html_artifact` MCP tool in `packages/server/src/mcp/mcp-server.ts` must run `bun scripts/smoke/check-artifact-sandbox-boundary.mjs` locally before pushing. It is the load-bearing real-Chromium probe for `docs/design/html-artifacts.md` §3's CSP sandbox boundary (premises P1/P2): it boots a disposable multi-user-mode server instance, creates a real artifact via a real MCP call, and drives a real headless browser through the opaque-origin document to confirm cookies, localStorage, credentialed same-origin fetch, external fetch, and external form POST are all blocked exactly as the design doc requires — plus same-run positive controls proving the walls are the sandbox's rather than the environment's. This check is a real-browser regression gate for a security boundary, not a sibling-test requirement, so it is not part of the `preflight-check.js` coverage patterns above.

Those same PRs must also run `bun scripts/smoke/check-artifact-server-story-e2e.mjs`, the real-HTTP server-story E2E for `docs/design/html-artifacts.md` §8's per-surface E2E (terminal half) and the create→serve→list→delete→serve round trip: it boots its own disposable multi-user-mode server instance, creates an artifact via a real `/mcp` JSON-RPC call (the actual terminal-agent-shaped call), verifies the artifact's stored `user_id` against the session's `createdBy` via a direct read of the disposable instance's SQLite file (the wire responses never expose `userId`), then drives the full `GET /:id` (exact CSP header) → `GET /` (list) → `DELETE /:id` → `GET /:id` (404) sequence as real HTTP. This check is a real-HTTP regression gate for the routes' shipping-path caller, not a sibling-test requirement, so it is not part of the `preflight-check.js` coverage patterns above.

**Verification tier (Discipline 4 of `os-environment-coupling.md`), both scripts above:** Unprivileged (no tier-2/3/4 residue; any machine with bun) — the real-Chromium requirement of `check-artifact-sandbox-boundary.mjs` is a browser dependency, not an elevation tier.

## Additional Verification: Fatal Incarnation Replacement Real-Tree E2E

PRs touching `handleEngineFatal` / `collectFatalIncarnation` / `fatalLeavesHarnessAlive` / the `fatalChainReplacementSpent` set in `packages/server/src/services/embedded-agent-worker-service.ts`, or that file's `deactivate` escalation, must run `bun scripts/smoke/check-fatal-incarnation-replacement.ts` locally before pushing. This is the real-process E2E for the defect where a `claude-sdk` worker's `claude` grandchild dies while its harness stays alive: it boots a disposable server instance with a real `AppContext` and a real `/mcp` on a real port, drives a real `claude-sdk` incarnation through a real `sh` -> `bun` -> `claude` tree, and SIGKILLs **only the grandchild** -- the one death shape that produces no OS exit for the server to observe. It then asserts the incarnation is replaced, the stranded processes are gone, the dead incarnation's MCP token no longer verifies **against the token registry itself** (no frame can substitute for that), and that the conversation survived the process boundary by recalling a word planted before the kill. A mid-turn kill covers the `turn-interrupted` marker, and a whole-tree kill in the same run is the positive control proving the healthy path was not what changed.

This is a real-process regression gate for a defect the unit layer structurally cannot catch -- the wedge only exists because a live harness hides a dead engine, and every fake spawn has an exit the observer sees. It is not a sibling-test requirement, so it is not part of the `preflight-check.js` coverage patterns above.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue) for `check-fatal-incarnation-replacement.ts` itself; its companion `check-fatal-incarnation-replacement-artifact-capture.ts` below is Unprivileged (no tier-2/3/4 residue; any machine with bun) — it has no `## Additional Verification:` section of its own.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user, like the SDK probes under `scripts/smoke/`. It is a manual gate, never a CI job.
- **It has a polarity mode.** `bun scripts/smoke/check-fatal-incarnation-replacement.ts -- --expect-brick`, run against a build whose fatal routing is removed, **asserts the bug reproduces** rather than merely tolerating a failure -- a run that silently recovers is reported as a polarity failure. Use it to confirm the apparatus still reaches the defect before trusting a green run, per `workflow.md`'s "A check's existence is not its detection power".
- **The run's worker NDJSON is captured before the disposable home is deleted.** The script's `finally` block copies every `outputs/<sessionId>/<workerId>.log` file out of the disposable `AGENT_CONSOLE_HOME` to `$HOME/.agent-console-smoke-captures/check-fatal-incarnation-replacement/<disposable-home-basename>/` — printed to stdout as `worker NDJSON captured to: ...` — strictly BEFORE `rmSync` removes that home. Read this **before** running the smoke, not after losing a run you wanted to keep: without this, every run's raw event stream is destroyed the moment the run ends, which is what happened to the reader shape recorded in Issue [#1468](https://github.com/ms2sato/agent-console/issues/1468) (observed in 1 of 6 runs, never captured, and unrecoverable once gone — see that Issue for the shape itself, deliberately not turned into a hand-built fixture). The capture is wrapped so its own failure (e.g. an unwritable destination) never affects the smoke's exit code or skips the disposable-home cleanup that follows it.
  - **Why `$HOME`, not `os.tmpdir()`.** `/tmp` is reboot-cleared on Ubuntu and may be swept by `systemd-tmpfiles` on an age basis; this smoke's billed runs also happen inside a delegate's worktree, itself deleted post-merge. Both are the same loss this Issue exists to stop — the NDJSON that prompted it (`/tmp/fatal3.log`) was already gone through exactly this route by the time anyone asked for it. `$HOME` survives both.
  - **Lifetime: not auto-pruned.** Each manual run adds one directory here. There is no cleanup job — runs are infrequent and small, and the operator who ran the smoke is expected to remove capture directories they no longer need (`rm -rf ~/.agent-console-smoke-captures/check-fatal-incarnation-replacement/<basename>`).
- **`bun scripts/smoke/check-fatal-incarnation-replacement-artifact-capture.ts` verifies the capture mechanism without a billed run.** It imports the exported `captureWorkerNdjson` function directly and exercises it against a synthetic disposable home containing a fake worker NDJSON file — no `AppContext`, no subprocess, no `claude` CLI. It is the Q13 proxy for this specific mechanism (`pre-pr-completeness.md`): the capture logic is pure filesystem plumbing sitting upstream of, and outside, the billable chain, so a billed run is not the right instrument to verify it with. It asserts the file survives at the capture destination byte-for-byte, that the disposable home is still removed on the default path, and that a forced capture failure (a same-named file blocking the destination directory) never changes the wrapping smoke's own exit code. Free to run, and worth re-running after any change to `captureWorkerNdjson` or the `finally` block's ordering — it is a manual gate like its billed sibling, not a CI job.

## Additional Verification: Idle Eviction Both-Polarities E2E

PRs touching `packages/server/src/services/embedded-agent-idle-eviction.ts`, the eviction machinery in `packages/server/src/services/embedded-agent-worker-service.ts` (`touchIdle` / `onIdleExpired` / `ensureDeliverable`, the `ready` / `evicting` / `evictable` runtime fields, or `handleExit`'s `reason` classification), or `EMBEDDED_AGENT_IDLE_EVICTION_MS` in `packages/server/src/lib/server-config.ts`, must run `bun scripts/smoke/check-embedded-agent-idle-eviction.ts` locally before pushing. This is the shipping-path E2E for idle eviction (Issue [#1412](https://github.com/ms2sato/agent-console/issues/1412)): it boots a disposable server instance with a real `AppContext` and a real `/mcp` on a real port, drives two real `claude-sdk` workers through real `sh` -> `bun` -> `claude` trees, and lets the real server-side timer evict one of them.

**It carries both polarities in one run, and the negative control is the point.** Subject A is activated first and control B later, so at the instant A's countdown elapses B is still inside its own — without that, "A was evicted" cannot be distinguished from "everything gets killed". The run asserts A's harness and its `claude` descendant are gone while B's are both alive, that A's persisted row is stamped `reason: 'evicted'` and reaches the global worker-exit callback as such, that B has no `exited` row at all, and that a message to the evicted A wakes it and **recalls a nonce planted before the eviction** on the same SDK session id. B, asked the identical question, must answer `UNKNOWN` — that control is what makes A's recall attributable to the resume rather than to a guessable prompt.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user — the `claude-sdk` builtin runs as the executing user and uses that user's own authentication, so there is no API key to configure. Roughly five real turns per run. It is a manual gate, never a CI job, which is why it is registered here but has no workflow.
- **`--idle-ms N` sets the threshold** (default 45 s) so a run takes minutes rather than the production half-hour. That substitution is upstream of and outside the chain under test: it changes how long the script waits to arrive at the eviction, never what eviction does.

**Do not weaken the graceful-exit assertion without re-measuring its reach.** The script asserts the `exited` row's `code` is `0` — that eviction went through the shutdown protocol rather than a signal — and that assertion exists because a mutation measurement showed the rest of the script could not tell the two apart. Replacing the deactivation call with a direct `SIGKILL` of the harness, which is exactly the hazard-line violation Issue [#1414](https://github.com/ms2sato/agent-console/issues/1414) makes dangerous, **passed all twenty other assertions**: the child dies anyway when it loses its stdio pipes, the exit observer still fires, and the reason is still `evicted`. Only the settle latency (1 ms against 253 ms) and the exit code moved, and the exit code is the one that separates them deterministically. The script's own comments carry that measurement and the negative control's known limit.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Idle Eviction, `openai-api` Revival Mechanism E2E

PRs touching the `isEvictableEngine` predicate or the `evictable` assignment in `packages/server/src/services/embedded-agent-worker-service.ts`, or the `openai-api` reconstruction path in `packages/embedded-agent/src/restore.ts`, must run `bun scripts/smoke/check-embedded-agent-idle-eviction-openai-api.ts` locally before pushing. This is the shipping-path E2E for extending idle eviction to `openai-api` (Issue [#1502](https://github.com/ms2sato/agent-console/issues/1502)).

**It is a sibling of `check-embedded-agent-idle-eviction.ts`, not an extension of it, because the two scripts verify different halves.** The eviction MECHANISM (countdown, commit-point re-check, `deactivate()` through the exit observer, `evicted` classification) is engine-agnostic and already proven by the `claude-sdk` script; duplicating those assertions here adds nothing. The REVIVAL mechanism is NOT shared: `claude-sdk` resumes the SDK's own session (`sdkResumed: true`, unchanged `sdkSessionId`); `openai-api` has no provider-side session concept at all and reconstructs the conversation from the persisted NDJSON log on every activation. That reconstruction path has a real bug history — see this file's "Restore E2E and smoke: the conversation must use a tool" section — so this script drives a tool-using planting turn through the `openai-api` reconstruction path across a real eviction, per that section's discipline, and asserts the mechanism used (`restoreInfo.sdkResumed === undefined` and `restoredMessageCount > 0`, not merely that the recall succeeded) rather than only the outcome.

The two-worker positive/negative-control shape (subject A evicted, control B activated later and still inside its own countdown) mirrors the `claude-sdk` script for the same reason: without B, "A was evicted" cannot be distinguished from "everything gets killed". B also serves as the recall control (never heard the nonce, must answer `UNKNOWN`), chosen over "a never-true fact asked of the same worker" because the threat here is guessability (a second worker sharing the filesystem could read the nonce file back before it is deleted), not confabulation by a resumed session — `openai-api` has no resumed session to confabulate about.

Two properties of the script that matter when re-running it:

- **It is billable and needs a provider key store** resolvable for `PROVIDER_KEY_REF` (default `opencode-go`, read from the single-user dev home; override with `PROVIDER_KEY_FILE`). A handful of small real HTTP turns per run. It is a manual gate, never a CI job.
- **`--idle-ms N` sets the threshold** (default 45 s), same substitution rationale as the `claude-sdk` sibling: it changes how long the script waits to arrive at the eviction, never what eviction does.

**It has a polarity mode.** `bun scripts/smoke/check-embedded-agent-idle-eviction-openai-api.ts -- --expect-not-evictable`, run against a tree with the `isEvictableEngine` fix reverted to `definition.engine === 'claude-sdk'`, asserts A is NOT evicted within its threshold — this is literally the pre-#1502 bug, so the polarity mode doubles as the "confirm the apparatus reaches the defect" check `workflow.md`'s "A check's existence is not its detection power" requires.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Restart-All Embedded-Worker Real-Provider E2E

PRs touching `SessionManager.restartAllAgentWorkers` in `packages/server/src/services/session-manager.ts`, or `EmbeddedAgentWorkerService.isEvicting` in `packages/server/src/services/embedded-agent-worker-service.ts`, must run `bun scripts/smoke/check-restart-all-embedded.ts` locally before pushing. This is the shipping-path E2E for restart-all's inclusion of embedded-agent workers (Issue [#1519](https://github.com/ms2sato/agent-console/issues/1519)): it boots a disposable server instance with a real `AppContext` and a real `/mcp` on a real port, drives two real `claude-sdk` embedded-agent workers through real `sh` -> `bun` -> `claude` trees, and calls the real `SessionManager.restartAllAgentWorkers()` -- the same method both `restart_all_agents` and `POST /api/sessions/restart-all-agents` call.

**It is modeled closely on `check-embedded-agent-idle-eviction.ts`, but the mechanism under test is different and so is what counts as proof.** Idle eviction is countdown-driven and needs a positive/negative pair staggered in time to prove selectivity; restart-all acts on every active worker in one synchronous sweep, so both workers here are activated together and BOTH are expected to be restarted by the same call. The two-worker shape survives for a different reason: it is the recall control (a second worker that never heard the planted nonce, chosen per `.claude/rules/test-trigger.md`'s "Restore E2E and smoke" threat table because the risk is guessability, not confabulation by a resumed session) rather than a selectivity control. "Fresh incarnation" is proven by a harness-pid comparison (before/after restart, must differ) rather than by liveness-then-death, since restart-all's positive claim is "replaced", not "evicted" -- and the pre-restart incarnation's persisted `exited` row is asserted `reason: 'managed'`, specifically to distinguish restart-all's ordinary deactivate from idle eviction's `'evicted'` classification.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user, same resolution as its idle-eviction sibling — the `claude-sdk` builtin runs as the executing user and uses that user's own authentication. Three real turns per run (plant, recall, recall-control). It is a manual gate, never a CI job.
- **No idle-threshold parameter.** Unlike the idle-eviction siblings, restart-all is not time-driven, so there is nothing analogous to `--idle-ms` to substitute.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Restore-Failure Declaration (R6) Smoke

PRs touching the R1 catch-block routing or the R6 write in `packages/server/src/services/embedded-agent-worker-service.ts` (the `preserved !== null` / fallback branch, or `writesDivergenceDeclaration`), `WorkerOutputFileManager.appendRestoreFailureMarker` / `resetWorkerOutput`'s `persistentMarkerLine` option in `packages/server/src/lib/worker-output-file.ts`, or the `restore-failure-declaration` / `restore-failure-boundary` members of `EmbeddedAgentServerEvent`, must run `bun scripts/smoke/check-restore-failure-declaration.ts` locally before pushing. This is the shipping-path smoke for R6 (#1447 stage 4, Issue [#1492](https://github.com/ms2sato/agent-console/issues/1492)): it boots a disposable server instance with a real `AppContext`, real DB-backed sessions/workers/definitions, and a real disk-backed `WorkerOutputFileManager`, drives the real `EmbeddedAgentWorkerService.activate` / `.deactivate` / `.sendUserMessage` through a forced restore-failure fallback, and asserts the persistent `restore-failure-declaration` marker is written into the fresh live file and survives a later, real second restore against it.

**FREE AND DETERMINISTIC -- the exception, stated explicitly because every other script in this section is billable.** No real `claude` CLI, no OS subprocess of any kind (`spawnAsUserFn` returns an in-process stub whose stdout/stderr the script pushes scripted NDJSON lines through -- including a real `sdk-session-id` line, so `worker.sdkSessionId` is set via the SAME production event handler a live incarnation would use, not a hand-written field mutation). R6's target property -- "the fallback path's marker gets written, and the next restore classifies it as noise, surviving into a later incarnation" -- is a pure filesystem-and-parsing property that does not depend on what a real LLM turn produces. It is registered here like its billable siblings, but, like them, is a manual gate, never a CI job -- the exception here is cost, not gate placement.

**The fault-injection mechanism is a chmod asymmetry, not a mock.** `appendRestoreFailureMarker` (R1's primary path) does `fs.appendFile` on the EXISTING live file, which needs write permission on the file itself; `resetWorkerOutput`'s fallback does `fs.rename` (needs write permission on the containing directory, not the file) followed by `fs.writeFile` creating a brand-new file at that path once the old one has been moved out (again directory-permission, unaffected by the old file's mode). Chmod'ing the live file to `0o444` before the second activation therefore fails ONLY the primary path's append, deterministically driving the fallback branch without touching any production code. The script observes (not assumes) that the fallback actually ran, via `getRestoreInfo(...).preservation !== 'in-band'`.

**It carries three cases in one run, and the two negative controls are the point.** Case 1 (positive: `claude-sdk` with a surviving `sdkSessionId`) writes the marker; Case 2 (`openai-api`, same forced-fallback sequence) and Case 3 (`claude-sdk` with `sdkSessionId` still `null`) write nothing -- without those controls, "the marker got written" cannot be distinguished from "the fallback always writes something".

**It has a polarity mode.** `bun scripts/smoke/check-restore-failure-declaration.ts -- --expect-no-declaration` wraps `workerOutputFileManager.resetWorkerOutput` to strip `persistentMarkerLine` from every call before delegating to the real implementation -- simulating "the R6 fix is absent" at the exact call site the fix uses, without touching source -- and asserts the fresh file is EMPTY. A run that finds the marker anyway under this flag is reported as a polarity failure. Use it to confirm the apparatus still reaches the defect before trusting a green run, per `workflow.md`'s "A check's existence is not its detection power".

**test-trigger.md's own "the conversation must use a tool" rule (below) does not apply to this script**, and its header comment says so explicitly: the established conversation in Case 1 never survives the forced reset that is the scenario under test, so no activation here ever reconstructs it, and there is no tool-call ordering to protect.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Unprivileged (no tier-2/3/4 residue; any machine with bun) — free and deterministic, no elevation, no billing.

## Restore E2E and smoke: the conversation must use a tool

**A conversation used to verify restore in an E2E or smoke must include at least one tool-using turn.** A text-only exchange cannot produce the event order a tool-using turn writes, and that order is where restore has actually broken.

The two engines disagree about it. `openai-api` emits an iteration's (possibly empty) `assistant-message` and then its `tool-call`s; `claude-sdk` emits a `tool-call` as soon as it observes one, flushing the assistant message afterwards, because an iteration opening with a tool use has no accumulated text to flush yet. The restore reader was written against the first shape and rejected the second, so **every `claude-sdk` turn that began with a tool call failed reconstruction and fell to the destructive reset** — on a two-turn conversation of thirteen lines, with no rotation involved.

Every shipped `claude-sdk` restore E2E and smoke was text-only. That is the whole reason the defect reached a user-facing surface with the feature otherwise fully covered: within a single process it works, and the failure appears only across a restart, only after a tool was used, and only as an absence.

In practice this costs a file and a sentence: write a nonce into the session's working directory and have the planting turn read it with `Read`, instead of putting the nonce in the message text. `scripts/smoke/check-embedded-agent-idle-eviction.ts` and `scripts/smoke/check-fatal-incarnation-replacement.ts` both do this, and are the worked examples.

### The recall assertion needs a negative control in the same run

**An assertion whose oracle is a model's generated text — a recall check, or anything read out of produced prose — must be accompanied, in the same run, by a control that fails when the fact under test is false.**

This is the reach measurement for a non-deterministic oracle, and it stands in the same place as mutation measurement for a deterministic pin. `workflow.md`'s standing rule is that a check's detection power is measured rather than assumed; what changes is the lever, and it changes with what the check reads:

| What the check reads | How its reach is measured |
|---|---|
| A deterministic system | **Mutate the code** — ordinary mutation measurement |
| A fixture's premise | **Assert the shape is present** — the tool-turn rule above |
| **A non-deterministic oracle** (a model's answer) | **Control the world** — neither code nor model can be mutated, so a negative control is the only lever left |

**Choose the control to match the threat; the two shapes are not interchangeable.**

- **The same question, asked of a second worker that never had the fact.** Controls for the question being answerable by anyone. `check-embedded-agent-idle-eviction.ts` uses this, because it already has a second worker and its risk is a guessable prompt.
- **A question about a fact that was never true, asked of the same worker.** Controls for that worker answering agreeably to anything — confabulation. `check-fatal-incarnation-replacement.ts` uses this, because it has one worker and a resume, and a resumed worker inventing a plausible answer is its specific risk.

**Be precise about what the control buys, because it is easy to credit it with more.** When the recalled token is unguessable — a random nonce, or a literal that exists only in the script — the recall assertion was never vacuous against blind invention in the first place: a model that never saw the token cannot emit it. The control does not rescue it from that. What it adds is narrower and still worth having: evidence that the worker returns a truthful negative when it genuinely lacks something, so a passing recall is not merely an agreeable answer.

**And check first whether the fact has a second route to the model.** A control cannot see one. If the nonce is also sitting in a file the worker can still read, the recall passes by reading it back, and no control over the model's answers detects that — the answer is genuine, it just came from somewhere the assertion's label does not claim. **Close the route (remove the file once the planting turn has used it), then pin that it stayed closed (assert the recall turn emitted no `tool-call`).** The first makes the assertion true; the second measures that it stays true.

**Absence assertions are the ones read-too-early makes pass falsely.** An absence check read early passes *because the thing has not happened yet*, identically whether or not it was going to. So an absence assertion is only as strong as the latest moment it could observe: **snapshot it after the boundary past which the event would no longer be written** (a turn's `idle`, a replacement's `ready`), never at the first sign the turn produced.

**A presence check is safe only when it is SCOPED to the current turn or incarnation** — by a baseline index, a slice, a **baseline-relative** occurrence count, or an identifier carried on the event. An unscoped presence check that scans accumulated history can be satisfied by an *earlier* event of the same type: a wait for "the replacement reported `ready`" written as `events.some(e => e.type === 'ready')` is satisfied by the first incarnation's `ready` and returns immediately, having observed nothing. Scoped forms that work: `.slice(marker).some(...)`, `const before = events.filter(...).length` then waiting for the count to exceed `before`, `.some(e => e.turnId === turnId)`. **A hard-coded count is not one of them**: `.filter(...).length >= 2` encodes an assumption about how many matching events history already holds, and passes immediately the moment that assumption is off by one. It is contextually safe at best -- correct in a run where exactly one such event precedes it, and silently vacuous in any run where two do.

**Defining the class by polarity is what makes a sweep possible, but a single-syntax grep is not the sweep.** `!x.some(...)` is one JavaScript form; the same assertion is written as `x.every(e => e.type !== 'k')`, `x.filter(...).length === 0`, `!x.find(...)`, `!s.includes(...)`, and a zero-length or `=== undefined` comparison. **Search for each known form — and treat that list as known forms rather than an exhaustive one**, because the next equivalent spelling is not in it. Enumerable beats a judgement like "reads too early"; it does not mean complete, and "I grepped, therefore I swept" is the failure this paragraph exists to block.

**That pin is also the boundary of this rule.** **Do not read this as "add more assertions over the event stream".** No assertion over emitted events could have caught the instance that produced this rule: the defect lived in a natural-language presupposition inside the question text, and the event stream is precisely where that is invisible. The tool-turn rule above is enforceable by asserting a shape; this one is not, which is why it needs a control rather than another pin. **But do not run the inference backwards.** "The premise is invisible to the event stream" does not license "recall assertions cannot be checked structurally": the second-route property above *is* visible there and must be pinned. Control what is genuinely unobservable; pin everything else.

(Lesson: both smokes had their planting turn routed through a tool in one change, which made "the secret word I told you" false in both. The eviction smoke's control broke loudly and forced an investigation; the fatal smoke's recall **passed, 18 of 18**, and forced nothing. The difference was not the defect, which was identical — it was how the model happened to answer once. **A green run there was a fact about one sampling of a non-deterministic answer, not a fact about the assertion.** It was found by review, after both an implementer and an orchestrator had read the run as clean.)

## Additional Verification: Image Attachment Vision E2E

PRs touching `packages/embedded-agent/src/attachment-content.ts`, the `EMBEDDED_AGENT_IMAGE_MIME_TYPES` / `MAX_IMAGE_ATTACHMENT_BYTES` constants in `packages/shared/src/constants/messages.ts`, the `attachments` field on `EmbeddedAgentCommand` / the persisted `user-message` server event (`packages/shared/src/{types,schemas}/embedded-agent.ts`), or the `provider.supportsImages` capability gate anywhere it is read (`agent-loop.ts`'s `supportsImages` dep, `main.ts`'s init wiring, `embedded-agent-worker-service.ts`'s pass-through), must run `bun scripts/smoke/check-embedded-agent-image-attachments.ts` locally before pushing. This is the shipping-path E2E for Issue [#1571](https://github.com/ms2sato/agent-console/issues/1571): it boots a disposable server instance with a real `AppContext` and a real `/api` + `/mcp` on a real port, then drives a real headless-Chromium screenshot of a nonce word through the REAL `POST /api/sessions/:sessionId/messages` multipart upload route -- the same endpoint the browser composer calls, not a shortcut through `sessionManager.sendMessage` -- against three real embedded-agent workers: an `openai-api` worker with `provider.supportsImages: true` (SUBJECT), an `openai-api` worker with `provider.supportsImages: false` (negative control for the capability gate), and the `claude-sdk-builtin` worker (unconditional, no gate).

**Why a real screenshot and not a hand-built PNG.** The Issue's own AC asks for "a real image containing a nonce word rendered as text (e.g. a screenshot of the word)". A synthetic solid-color PNG would exercise the base64 / content-part plumbing but never the thing a vision model actually has to do -- read text out of pixels -- so it would prove the wire, not the feature.

**The negative control IS the polarity check here**, unlike this section's `--expect-*`-flag siblings: the capability gate (`supportsImages: false`) has a naturally-occurring negative case built into the feature itself, so no separate "revert the fix and confirm the bug reproduces" mode is needed. The SUBJECT/CONTROL pair is a capability-gate control (does the flag actually suppress image parts?), not a selectivity-over-time control like the idle-eviction siblings' A/B pair.

**Operational gotcha for anyone re-running this, measured on the delegate's own machine and more precise than "Chromium can only write under `$HOME`":** the snap-packaged Chromium's `home` interface denies writes to DOT-PREFIXED directories under `$HOME`, not just paths outside `$HOME` altogether. `chromium --screenshot=$HOME/.foo/out.png` fails SILENTLY at the shell level (the process can still exit 0) with `Permission denied (13)` buried in Chromium's own stderr; reading FROM a dot-directory works fine, only writing is denied. The script works around this by having Chromium write into a plain (non-dot) staging directory under `$HOME` first, then copying the result into this repo's established dot-prefixed capture convention (`~/.agent-console-smoke-captures/check-embedded-agent-image-attachments/<run-id>/`, same convention as `check-fatal-incarnation-replacement.ts`) via ordinary `node:fs` calls, which are not subject to Chromium's sandbox. The staging directory is removed once the copy completes; the capture directory is **not auto-pruned** (same convention as this file's other capture directories -- the operator's responsibility to clean up).

Two properties of the script that matter when re-running it:

- **It is billable and needs both a provider key store** (resolvable for `PROVIDER_KEY_REF`, default `opencode-go`, read from the single-user dev home; override with `PROVIDER_KEY_FILE`) **and a real, authenticated `claude` CLI** for the invoking OS user (the `claude-sdk` case). A handful of small real turns across three workers per run. It is a manual gate, never a CI job.
- **Headless Chromium is required** at `CHROMIUM_BIN` (default `/snap/bin/chromium`, overridable).

Each case additionally asserts the persisted `user-message` server event actually carries `attachments: [{ path, mimeType: 'image/png' }]` for the message that used the image -- a passing recall is not credited to the model unless the attachment demonstrably reached the wire.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Restore-Boundary Usage-Seed Real-Provider E2E

PRs touching `findRestoredUsageSeed` / `reconstructConversation`'s `usageSeed` in `packages/embedded-agent/src/restore.ts`, `AgentLoop`'s `resolveRestoreBoundaryUsage` / `compactAtRestoreBoundaryIfNeeded` in `packages/embedded-agent/src/agent-loop.ts`, or the `restoredUsage` field on the `init` command (`packages/shared/src/{types,schemas}/embedded-agent.ts`, and where `packages/server/src/services/embedded-agent-worker-service.ts` composes it) must run `bun scripts/smoke/check-restore-boundary-usage-seed.ts` locally before pushing. This is the real-provider E2E for the restore-boundary compaction check: it boots a disposable server instance with a real `AppContext` and a real `/mcp` on a real port, creates a real `openai-api` definition with a deliberately conservative declared window, grows a conversation with **real billed turns** until the provider's own reported `prompt_tokens` clears the threshold, then restarts the worker and asserts the activation decided on the persisted **measurement** rather than on `estimateTokensFromChars`.

The reason a unit test cannot substitute: the defect is a disagreement between two numbers, and only one of them is ours. A fake adapter can be told to report anything, which makes the gap an input rather than a measurement — so the unit layer can pin the extraction rule and the max rule, but only the real chain establishes that the gap exists at the size the fix assumes. Measured 2026-08-29 on a 12,000-token declared window: the estimator read **5,450** where the provider reported **10,512** for the same conversation.

Two properties of the script that matter when re-running it:

- **It is billable and needs a provider key** resolvable for `PROVIDER_KEY_REF` (default `opencode-go`, read from the single-user dev home; override with `PROVIDER_KEY_FILE`). It is a manual gate, never a CI job. A run is a handful of small turns — cents, not dollars.
- **It has a polarity mode.** `bun scripts/smoke/check-restore-boundary-usage-seed.ts -- --expect-underfire`, run against a tree whose fix is removed (`git checkout origin/main -- packages/` and back), **asserts the defect reproduces** rather than merely tolerating a failure — a run that silently compacts is reported as a polarity failure. Use it to confirm the apparatus still reaches the defect before trusting a green run, per `workflow.md`'s "A check's existence is not its detection power".

**What it does not reach, and why that is arithmetic rather than effort.** The Issue's full wedge ends in a provider 400 no `Compact` can escape. Reaching it needs the declared window to be both honest and small: with `G` the tool-schema gap and `T` the threshold, `W < G / (1 - T)` — about 37,500 tokens at the measured defaults. The smallest context window across the dev instance's provider catalogue is 196,608, and that model silently truncates rather than erroring, so the 400 is unreachable there. The downstream link (a turn ending in error settles no compaction) is pinned in `packages/embedded-agent/src/__tests__/agent-loop-compaction.test.ts` instead, where a turn's ending is directly constructible.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Compaction Identifier-Fidelity Probe

PRs touching `DEFAULT_COMPACTION_PROMPT` / `loadCompactionPrompt` in `packages/embedded-agent/src/compaction-prompt.ts`, or `AgentLoop.compact()`'s distillation call in `packages/embedded-agent/src/agent-loop.ts`, should run `bun scripts/smoke/probe-compaction-fidelity.ts` locally before pushing (measurement, not a pass/fail gate — see below). This is the real-provider measurement for Issue [#1350](https://github.com/ms2sato/agent-console/issues/1350): whether conversation-local identifiers stated as **ordinary facts, with no preservation coaching**, survive a real `openai-api` compaction boundary verbatim. It boots a disposable server instance with a real `AppContext` and a real `/mcp` on a real port, plants three synthetic identifiers (a run id, a filename, a change reference) in a single uncoached turn, drives real pressure-round turns until a real `context-compacted` boundary fires, and reads the boundary's own persisted `summary` field — the actual distillation text the model produced — for each identifier's presence.

**Why this is a measurement, not a gate.** Per the Issue's own ruling: "a post rate not better than pre is a finding to report, not to hide." The script never fails on a low retention rate — it exits 0 whenever every requested run reached a real boundary and its summary was read, regardless of what fraction of identifiers survived. A non-zero exit means the *harness* could not produce a measurement (a run never reached a boundary within its round/time budget, a turn errored or timed out), which is a different fact from a low rate.

**Why this is the real-provider chain and not a unit test.** The retention outcome is a property of what a real LLM does when asked to distil a real conversation under a real prompt — no fake adapter's output can stand in for that without begging the question the Issue is about. There is currently no CI-level pin on the prompt's composition, since no preservation clause has shipped; this script is the only measurement that exists for this prompt today.

Two properties of the script that matter when re-running it:

- **It is billable and needs a provider key**, same resolution as `check-restore-boundary-usage-seed.ts` (`PROVIDER_KEY_REF`, default `opencode-go`, read from the single-user dev home; override with `PROVIDER_KEY_FILE`). It is a manual gate, never a CI job. A run is a handful of small turns per `--n` repetition — cents, not dollars.
- **`--prompt-file <path>` selects the arm.** Omitted, the run measures the CURRENT bundled `DEFAULT_COMPACTION_PROMPT` (the PRE arm). Passed, the run measures the file's content instead, delivered through the exact same repo-layer override path `loadCompactionPrompt()` reads in production (`<cwd>/.agent-console/compaction-prompt.md`) — there is no test-only fork of the loader. `--n <count>` repeats the arm and aggregates a retention rate; comparing a PRE run against a POST run (pointed at a candidate revised prompt) is how a change to the clause is evaluated before it ships.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: SDK Instruction-Loading Premise Probe

Before changing the SDK arm's `settingSources` value in `packages/embedded-agent/src/sdk-engine.ts`'s `buildOptions`, or before relying on any claim about what the SDK's native `settingSources: []` does or does not suppress, run `bun scripts/smoke/probe-sdk-instruction-loading.ts` against a real, authenticated `claude` CLI session. This is the Task 0 gate probe for Issue [#1343](https://github.com/ms2sato/agent-console/issues/1343)'s scoped section (project-instruction parity on the SDK arm): it drives two real SDK sessions against a scratch git repo containing three unguessable canary tokens — `CLAUDE.md` (canary A), `.claude/rules/unscoped.md` with no frontmatter (canary B), and `.claude/rules/scoped.md` with `paths: ["src/**"]` (canary C) — and asks the model, before and after a real `Read src/x.ts` tool call, whether it knows any of the three.

**What the two arms prove.** `--off` (`settingSources: []`, what ships today) asserts the whole Phase A design's load-bearing premise: that this setting suppresses the SDK's native project-instruction discovery ENTIRELY (CLAUDE.md and any native `.claude/rules/*.md` auto-discovery), both before and after a path-matching tool call — a leak of canary B or C is a STOP condition, not a tolerable finding, because it means the SDK loads rules independently of `settingSources` and Phase A's custom loader-driven rules layer would be redundant with (or in conflict with) a native one already running underneath it. `--project` (`settingSources: ['project']`) is the positive control: it proves the canaries are actually detectable at all, and separately documents the native CLI's own lazy scoped-rule activation semantics (A and B known at session start, C unknown until the matching-path `Read` fires) — the empirical basis Phase B's `PostToolUse`-hook design premise rests on.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user — the `claude-sdk` engine runs as the executing user and uses that user's own authentication, so there is no provider key to configure. Six small turns per full run (three per arm). It is a manual gate, never a CI job.
- **`--off` / `--project` select an arm**; omitted, the default is both, in order.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: SDK PostToolUse Context-Injection Premise Probe

Before implementing the `PostToolUse` hook activation path in `packages/embedded-agent/src/sdk-engine.ts` for Issue [#1343](https://github.com/ms2sato/agent-console/issues/1343) Phase B (lazy activation of scoped `.claude/rules` on the claude-sdk engine), or before relying on any claim about whether a `PostToolUse` hook's `additionalContext` return value reaches the model, run `bun scripts/smoke/probe-sdk-post-tool-use-context.ts` against a real, authenticated `claude` CLI session. This is the Task 0 gate probe for Phase B's design premise: a `PostToolUse` hook is how a scoped `.claude/rules` entry gets injected lazily, the first time a matching tool call fires, rather than being loaded eagerly at session start.

**Three arms, and what each proves.** `--a` (SUBJECT) registers a `PostToolUse` hook matching `Read` that fires once and returns `additionalContext` containing a nonce; the turn sequence asks for the nonce before any tool call (expect: unknown), triggers `Read src/x.ts`, then asks again (expect: known). `--b` (NEGATIVE CONTROL) is mechanically identical but the hook returns only `{ continue: true }`, no `additionalContext` — this is what makes Arm A's result attributable to `additionalContext` specifically rather than to the model guessing or some other artifact of merely registering a matching hook. `--c` (SIZE-CEILING MEASUREMENT) repeats Arm A's shape but wraps the nonce inside ~40 KB of deterministic filler, recording whether the nonce still arrives at that size (the vendored SDK's `sdk.d.ts` documents no cap on `additionalContext` itself, unlike the neighboring `classifierContext` field's documented 2000-code-unit cap).

**STOP condition.** If Arm A fails while Arm B behaves as expected (B does not leak), the `PostToolUse` hook does not deliver `additionalContext` to the model on this build, and Phase B's claude-sdk arm takes the recorded fallback (R6): eager inclusion instead of lazy `PostToolUse`-hook injection. If Arm B leaks the nonce, the probe itself is unreliable, not the SDK, and Arm A's result cannot be trusted until that is understood.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user — the `claude-sdk` engine runs as the executing user and uses that user's own authentication, so there is no provider key to configure. Nine small turns per full run (three per arm: ask#1, the Read, ask#2). It is a manual gate, never a CI job.
- **`--a` / `--b` / `--c` select an arm**; omitted, the default is all three, in order.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: SDK Live Effort-Apply Premise Probe

PRs touching `SdkEngine.setModelParams` in `packages/embedded-agent/src/sdk-engine.ts` (the `query.setModel` / `query.applyFlagSettings({ effortLevel })` pair, or the `model-params-applied` reporting around it), or relying on any claim about what `applyFlagSettings` does to a live session's reasoning effort, must run `bun scripts/smoke/probe-sdk-effort-live-apply.ts` against a real, authenticated `claude` CLI session. This is the premise probe behind the mid-run effort path of Issue [#1521](https://github.com/ms2sato/agent-console/issues/1521) Phase 3.

**The refuted premise, which is the whole reason this script exists.** The design for that phase was written on the assumption that the SDK has NO runtime effort setter: a model change would go through `Query.setModel`, and an effort change would have to be applied by RESTARTING the worker in place (`deactivate` then `activate`, with the persisted row re-read at `init`), the engine reporting an `effort-requires-restart` refusal on the `model-params-applied` event and the UI warning the user that changing effort restarts the agent. **This probe measured the opposite, and that entire branch — the refusal reason, the server-side restart-in-place, and the UI sentence — was removed before it shipped.** Do not read the script as redundant with `sdk-engine.test.ts`: the unit tests pin what our code CALLS, and this is the only thing in the repository that measures what the SDK DOES with it. A future SDK bump that changes this behavior surfaces here or nowhere.

**Three arms, and what each measures.** `--set` (SUBJECT) constructs a query with `Options.effort: 'low'`, spends a control turn, then calls `applyFlagSettings({ effortLevel: 'high' })` and spends a second turn: `high` on turn 2 means the flag layer overrode a query-time effort on a live session. `--clear` answers what CLEARING falls back to — `'low'` at construction, then `'medium'`, then `null` — and `'medium'` is deliberate rather than incidental: `'high'` is simultaneously a plausible set-value and the SDK's own default, so it conflates "the clear was ignored" with "the clear worked and landed on the default", while `'medium'` separates `low` (stale query-time value), `high` (SDK default) and `medium` (clear ignored) cleanly. `--absent` (BASELINE) omits `Options.effort` entirely for one turn, giving the level a fresh session with no override runs at — which is exactly the state the discarded restart-based design would have produced, so `--clear`'s turn 3 read against it is what shows a live clear and a restart to be the same state or not.

**The positive control is a gate, not a formality.** Turn 1 of the `--set` and `--clear` arms must observe `low`; if it does not, the observables do not track `Options.effort` on that build and the arm reports INCONCLUSIVE rather than a verdict. The observables are a `PostToolUse` hook's `input.effort.level` (documented in the vendored `sdk.d.ts` as the active level for the current turn, after any silent per-model downgrade) cross-checked against `echo $CLAUDE_EFFORT` from a `Bash` call; the model is never asked what effort it is using. A call to `applyFlagSettings` that returns without throwing is NOT evidence — `scripts/smoke/probe-sdk-compaction.ts` measured that same call honouring `autoCompactEnabled` while silently ignoring `autoCompactWindow`, so only an observed effect counts.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user — the `claude-sdk` engine runs as the executing user and uses that user's own authentication, so there is no provider key to configure. Six small turns per full run (two for `--set`, three for `--clear`, one for `--absent`). It is a manual gate, never a CI job.
- **`--set` / `--clear` / `--absent` select an arm**; omitted, the default is all three, in order.
- **Four exit codes, and they separate the two outcomes that matter.** `0` = measured, and PS9 HOLDS. `1` = INCONCLUSIVE — a failed positive control, an `applyFlagSettings` that threw, a turn that did not settle, or a run that selected no arm bearing on PS9 (a `--absent`-only run measures a baseline and says nothing about the premise). `2` = HARNESS failure, so nothing about the SDK was measured. `3` = measured, and PS9 is REFUTED. A conclusive refutation outranks an inconclusive sibling arm, because the refutation is a measurement in its own right and is precisely what this script exists to surface on an SDK bump. The mapping is pinned by `scripts/smoke/__tests__/probe-sdk-effort-live-apply.test.ts`, which imports the verdict function directly and never runs the billed measurement.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: SDK Auto-Memory Premise Probe

Before writing the Phase 2 design for epic [#1636](https://github.com/ms2sato/agent-console/issues/1636)'s auto-memory work, or before relying on any claim about whether the SDK's `autoMemoryEnabled` / `autoMemoryDirectory` / `SDKMemoryRecallMessage` mechanism reads, writes, leaks, or redirects on a headless `claude-sdk` session, run `bun scripts/smoke/probe-sdk-auto-memory.ts` against a real, authenticated `claude` CLI session. This is the Task 0 gate probe for Issue [#1658](https://github.com/ms2sato/agent-console/issues/1658): epic #1636's gap 2 framed auto-memory as "unreachable" from the `claude-sdk` arm, citing #1348 — a real but accidental, single-session observation with no reproducible methodology. `packages/embedded-agent/src/sdk-engine.ts`'s `buildOptions` set neither field when this probe was first written, so some undeclared SDK default was very likely already active on every `claude-sdk` worker, unconfigured and unobserved; this script replaced the accident with a controlled measurement. Production has since moved: `buildOptions` now explicitly sets `settings.autoMemoryEnabled: false` (Issue #1681, see this section's `--auto-memory-off` bullet below), so the probe's job is no longer discovering an undeclared default — it is confirming the SDK actually honors the declared one, which is a distinct question the explicit setting does not answer by itself.

**Two corrections to the Issue's own wording, discovered while implementing this script and confirmed by the Architect against the vendored `sdk.d.ts`.** The Issue asked Arm B to capture a `memory_type` field on `SDKMemoryRecallMessage` (`'User'|'Project'|'Local'|'Managed'`); that field does not exist there — it belongs to a different, unrelated type (`InstructionsLoadedHookInput`, the CLAUDE.md/rules loader hook). What `SDKMemoryRecallMessage.memories[]` actually carries is `scope: 'personal'|'team'|'organization'` (plus a top-level `mode: 'select'|'synthesize'`), which is in fact the more direct answer to Arm B's actual privacy-scope question. Separately, `autoMemoryEnabled` / `autoMemoryDirectory` are not direct `Options` fields — they live on the `Settings` interface, reached via `Options.settings?: string | Settings`, the same nesting the sibling probes already use for `autoCompactEnabled`.

**A third adaptation: the sanitized-cwd algorithm cannot be read from source.** The auto-memory logic is not present anywhere in the vendored `@anthropic-ai/claude-agent-sdk` npm bundle (confirmed by grepping `sdk.mjs` for `memory_recall` and for a sanitizer — zero hits); it lives inside the `claude` CLI binary the SDK spawns. Rather than guess a sanitizer, every arm that needs the SDK's own default `autoMemoryDirectory` for a given scratch `cwd` discovers it empirically at runtime: run one turn with `autoMemoryEnabled: true` and diff the isolated `CLAUDE_CONFIG_DIR/projects/` listing to learn the directory the SDK itself created.

**Four arms, each against a disposable scratch cwd and its own throwaway, isolated `CLAUDE_CONFIG_DIR` — no real user memory directory is ever touched**, by the same construction every sibling probe's own `verifyIsolation()` check relies on for session storage generally:

- **`--a` RECALL.** Seeds a nonce as an index-linked topic file (per the documented contract at [code.claude.com/docs/en/memory](https://code.claude.com/docs/en/memory), fetched directly rather than inferred by analogy — Architect ruling, PR #1662; the cited page documents the MEMORY.md-index-plus-topic-file architecture in prose only, with no literal syntax example — the index-line and frontmatter shape specifically are taken from Claude Code's own built-in auto-memory system-prompt instructions instead) at the SDK's own default Project-scope memory directory (path discovered per the adaptation above), ask a fresh session at the same cwd whether it knows the fact. Observes both the MECHANISM (`SDKMemoryRecallMessage`, path-matched against the seeded topic file so "recall happened" means "recall happened for the format assumed") and the OUTCOME (the model's answer), with a negative-control cwd (nothing seeded) in the same run — a control failure (the control cwd itself recalls or answers with the nonce) gates the reading exactly like `probe-sdk-effort-live-apply.ts`'s positive control gates its own. **A `RECALL DOES NOT WORK` verdict from this arm must be read alongside Arm C's independently observed write-file layout in the findings comment** before being cited as a premise refutation — if Arm C's own write lands in a different shape than this arm assumed, that is evidence the assumed seeding format needs revisiting, not that recall itself is broken (same interpretation-time-cross-reference shape as Arm B's scope caveat below).
- **`--b` LEAK.** At a cwd with nothing ever seeded, asks a broad "what do you know about me" question and reports whether anything surfaces and, if so, its `scope`/`mode` — re-deriving #1348's finding under controlled conditions. `content` is never logged, by construction (`redactRecallEntry`): only `scope`/`mode` and whether the surfaced path lies within this run's own isolated config dir (this run's own scratch data) or not (a real leak from elsewhere). **Scope limitation (Architect ruling, 2026-09-13):** because every arm runs under an isolated `CLAUDE_CONFIG_DIR`, this arm can only observe a recall whose `path` is NOT rooted under that throwaway dir (`organization`, in principle `team`) — the `'personal'`/User-scope channel #1348 actually observed is unreachable by this arm's design, so a `NO LEAK` result must never be read as covering User-scope; see Issue [#1663](https://github.com/ms2sato/agent-console/issues/1663) for the tracked follow-up.
- **`--c` WRITE.** Tells the model something worth remembering, polls for a file to appear under the default directory within a bounded timeout (auto-memory write may have its own gate assuming an interactive TUI a headless SDK session can't satisfy — unknown until measured).
- **`--d` REDIRECT.** Sets `autoMemoryDirectory` explicitly and confirms both a read and a write land at the configured path — and that the write does not ALSO leak to the default location, which is what makes the override exclusive rather than merely additive.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user — the `claude-sdk` engine runs as the executing user and uses that user's own authentication, so there is no provider key to configure. Roughly 6-10 small turns across the four arms, plus bounded local-filesystem polling for write detection. It is a manual gate, never a CI job.
- **`--a` / `--b` / `--c` / `--d` select arms**; omitted, the default is all four, in order. **`--expect-no-recall` (arm A) and `--expect-no-write` (arm C) are polarity flags**, not arm selectors — each runs its arm with the seeding/write-prompting step deliberately skipped and confirms the probe's OWN detection correctly reports absence, per `workflow.md`'s "a check's existence is not its detection power" applied to the probe's own apparatus before its numbers are trusted. They only take effect when their arm is also selected (explicitly, or via the all-four default).
- **Three exit codes, and this is a measurement script, not a pass/fail gate** — same shape as `probe-compaction-fidelity.ts`. `0` = harness ran to completion; every requested arm produced a definite measurement, regardless of which way any individual measurement came out. `1` = INCONCLUSIVE for at least one requested arm. `2` = HARNESS failure — nothing was measured. The mapping, plus each arm's own verdict classifier and the privacy redaction function, are pinned by `scripts/smoke/__tests__/probe-sdk-auto-memory.test.ts`, which imports them directly and never runs the billed measurement.

**Task 0b (Issue [#1667](https://github.com/ms2sato/agent-console/issues/1667)) extends this same script with three more arms — `--e` / `--f` / `--g` — after Task 0's own retroactive caveat found a confound arms A-D never accounted for: none of them ever set `Options.systemPrompt`, and what an omitted `systemPrompt` resolves to is undocumented in the vendored `sdk.d.ts`. If it means "no preset", the model in every arm A-D run may never have been told the feature exists — which alone would explain every REFUTED verdict, independent of any timescale or switch. Arms E/F/G separate awareness, switches, and timescale, cheapest first:**

- **`--e` AWARENESS.** Seeds the same MEMORY.md-index-plus-topic-file shape as arm A (via the shared `seedMemoryTopic` helper both arms now call — never a second copy of it), with a NONCE-BEARING index title (arm A's own fixed title is guessable from this repo's source; a nonce is not). Runs one combined turn per `systemPrompt` configuration — (i) omitted entirely (arms A-D's and production's own shape today), (ii) `{ type: 'preset', preset: 'claude_code' }`, (iii) the same preset with `excludeDynamicSections: true` as a negative control — asking both required observables at once: (1) LOCATION, "quote the absolute path of your auto-memory directory", and (2) ACCESS, "what is the title of the entry in your memory index?". A four-way classifier (`classifyArmEConfig`) reads the two together per configuration — both hit -> aware-and-reading, ACCESS only -> aware-prose-only (NOT absence — this is what stops a prose-only dynamic section from being misread as a REFUTED premise), LOCATION only -> told-not-read (a real finding on its own), neither -> unaware. **The (iii) control is evaluated honestly, not assumed clean**: the vendored `sdk.d.ts` documents that `excludeDynamicSections` re-injects the stripped content as the first user message, so if either observable still surfaces under (iii), the script reports `control: NONE AVAILABLE` rather than treating the negative control as trustworthy. The `SDKControlGetContextUsageResponse.memoryFiles` accessor (the camelCase, harness-wired sibling of the Issue's own `SDKContextUsage.memory_files` — see the script header for the correction) is read as a bonus mechanism observable on these same turns via `pollUsage: true`, logged but not gating the classification.
- **`--f` SWITCHES.** Picks whichever of (i)/(ii) is classified as NOT `unaware` first — LOCATION awareness alone counts, not just ACCESS, since arm F's premise is "awareness exists under configuration X" (`--f-config <omitted|preset>` overrides explicitly when arm E did not run in the same invocation, defaulting to `omitted`). Under that configuration, sets `Options.settings.autoMemoryEnabled: true` AND `autoDreamEnabled: true` explicitly, then re-runs arm C's write check and arm A's recall check UNCHANGED via the same shared cores (`runWriteCheck` / `runRecallCheck`) both original arms now call.
- **Arm F/G halt by default, ONLY when no configuration shows awareness (Architect ruling, PR #1676 review, refining an earlier owner ruling).** When arm E ran in the same invocation and shows NEITHER (i) nor (ii) carries awareness, arm F reports a `HALTED` verdict instead of spending turns — measuring the switches against a model that cannot know what to write would be uninterpretable, and arm E's own verdict stays `conclusive` so the run still exits `0`. Arm G reports its own SKIPPED verdict in turn (it needs arm F's write result). **A dirty or unsettled (iii) control does NOT halt**: per the vendored `sdk.d.ts`, `excludeDynamicSections` is documented to re-inject stripped sections as the first user message, so (iii) surfacing an observable is the EXPECTED result on a doc-conformant build — treating it as a halt condition would make F/G unreachable without `--force-f` on any build that behaves as documented. A dirty (iii) is instead a caveat on ATTRIBUTING (i)/(ii)'s awareness to the dynamic section specifically (already stated in arm E's own note), not a precondition for F to run. `--force-f` overrides the awareness halt for a deliberate operator run anyway; a standalone `--f` run with no `--e` in the same invocation is unaffected by this gate.
- **`--g` TIMESCALE.** Re-runs arm F's write check ONLY, with a longer poll via `--extended-timeout <ms>` (omitted or `0` behaves exactly like arm F's 60s poll). Runs ONLY when arm E showed awareness under PRODUCTION's configuration (`omitted`) AND arm F still showed no write — selecting `--g` therefore always also selects `--e` and `--f` as prerequisites, printing a SKIPPED verdict when the gate is not met rather than running. **Owner directive, 2026-09-13, binding: `--extended-timeout` is capped at 5 minutes (300000ms) regardless of what is requested** (`EXTENDED_TIMEOUT_CAP_MS`); the script clamps and warns rather than trusting the caller. No teardown arm, no recall re-check. The shared write-poll loop (`pollForContent`) makes no repeated LLM turns — confirmed by reading it before this arm was dispatched, per the Issue's own instruction — so a longer window costs wall-clock only, never additional billed turns.
- **Order confound closed (Architect ruling, PR #1676 review).** (i)/(ii)/(iii) run sequentially against the SAME memory dir with `autoMemoryEnabled: true`, so a write during one configuration's turn could change what the next configuration observes. The memory dir is deleted and reseeded identically (same title, same nonce) immediately before every configuration's turn — zero extra turns — and the memory-dir file count is logged both after reseeding and after the turn.
- **Real config-location cross-check, run around every arm (not once at the end), with an attribution filter (Architect ruling, PR #1676 review).** Before ANY arm runs (arms A-D included), the script resolves where the OPERATOR's real, non-isolated `CLAUDE_CONFIG_DIR` lives (the env var if already set, else `~/.claude` — measured via existence, never assumed), recursively snapshots every file's mtime under it, and writes a small canary file there (named `probe-sdk-automem-canary-<runId>.tmp`, matching the `PROBE_SLUG` constant, `probe-sdk-automem`) as an in-band positive control. **This canary write is the ONE thing this script writes outside its own isolated per-arm config dirs** (owner ruling: compliant with `os-environment-coupling.md` Discipline 2 — same operator, project-scoped write to that operator's own home, self-cleaned). After every selected arm has run, the script snapshots again — but only a path ATTRIBUTABLE to this run counts toward escalation: its own path contains `PROBE_SLUG` (path-wide — the canary, or any probe-named scratch artifact), OR its post-run content contains one of the nonces this run minted (tracked via `trackedNonce`) WITHIN a `memory/` path segment specifically. **The `memory/` scoping is load-bearing (Architect ruling round 2, PR #1676 review):** this probe prints every nonce to stdout, and the delegate session running it ingests that stdout as its own tool output, writing it into ITS OWN transcript `.jsonl` under the same real config dir — an unscoped nonce-content match would make the operator's own conversation transcript "attributable" and false-escalate on every single run. A transcript is never under `memory/`; only genuine auto-memory content is. Everything not attributable is logged as "unrelated concurrent activity: N paths, of which M echoed a run nonce outside any memory/ dir" and never escalates, since the same OS user's OTHER live Claude Code sessions write their own transcripts continuously and a raw "did anything change" diff would false-positive on any host with concurrent activity (precedent: Task 0's own manual real-tree addendum found nonce matches only in top-level session transcripts, explicitly not under `memory/`, and dismissed them as ordinary conversation — this filter mechanizes exactly that distinction). The canary is classified through this SAME filter, not a raw diff check, so its positive control proves the filtered detector works. A failed positive control or an attributable change escalates the script's own exit code to `HARNESS` regardless of what the arms measured. The canary is removed in a `finally` block.
- **E/F/G are NEVER part of the bare no-flags default** (still arms A/B/C/D only, in order) — select them explicitly. `--expect-no-write` threads into arm F's and arm G's write halves exactly as it does for arm C; arm E's polarity is its own (iii) control, not a separate flag.
- Billing: arm E is three single turns (~$0.05), arm F is two turns (~$0.10) when not halted, arm G (only when its gate is met) reuses arm F's turns plus up to 5 minutes of wall-clock polling, no additional billed turns.
- **`--auto-memory-off` (Issue #1681) is a standalone, non-lettered measurement, independent of arms A-G and NEVER part of the bare default.** Includes an in-run positive control before the measurement: the same seeded dir, configuration (i), `autoMemoryEnabled: true`, classified via `classifyArmEConfig` and REQUIRED to read `aware-and-reading` — any other classification (including an unsettled control turn) reports `INCONCLUSIVE -- control failed`, since a silently-changed ON behaviour would make the OFF result below uninterpretable, and the OFF turn is skipped entirely in that case (`combineAutoMemoryOffVerdict`). The memory dir is reseeded identically before the measured OFF turn (`autoMemoryEnabled: false`), expecting the mechanism silent on all three observables at once (`memoryFiles=[]`, no ACCESS hit, no LOCATION hit — `classifyAutoMemoryOffCheck`). Task 0b's `aware-and-reading` result (arm E, configuration (i), `autoMemoryEnabled: true`) is corroborating evidence from a separate run, not the control itself — the control now lives in the same run. Discovery + ON control + OFF turn = roughly $0.15 total.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Instruction-Loader Parity E2E, Both Engines

PRs touching `loadInstructions` / `loadRulesLayer` / `parseRuleFrontmatter` / `RULES_LAYER_CAP_BYTES` in `packages/embedded-agent/src/system-prompt.ts`, `main.ts`'s `initializeLoop` (either init arm's instruction-loading call), or `packages/server/src/services/embedded-agents/claude-sdk-builtin.ts`'s `instructions`/lack thereof, must run `bun scripts/smoke/check-instruction-loader-parity-e2e.ts` locally before pushing. This is the shipping-path E2E for Phase A (project-instruction parity on the SDK arm; Issue [#1343](https://github.com/ms2sato/agent-console/issues/1343)): it boots a disposable server instance with a real `AppContext` and a real `/mcp` on a real port, drives one real `openai-api` worker and one real `claude-sdk` worker (both real embedded-agent subprocesses, both against the SAME scratch git repo), and asks each a single first-turn question about three pieces of content: a `CLAUDE.md` nonce (the pre-existing chain layer, re-confirmed as a no-regression baseline), an unscoped `.claude/rules/*.md` nonce (R2, new), and a negative-control nonce planted under `.claude/rules-not/*.md` (same file shape, wrong directory name -- must never be honoured on either engine).

**Why a single first turn, no tool call.** Unlike this file's restore/eviction smokes, this content is loaded EAGERLY into the system prompt at activation -- it is not something a later tool call or a resumed session reveals, so there is no "recall across a process boundary" here and no need for the tool-turn discipline or a recall negative control. The `.claude/rules-not` nonce is the negative control this AC item asks for, and it is a directory-scoping control (does the loader only ever scan `.claude/rules`?), not a recall-attribution control -- a materially different question from the ones the tool-turn rule above governs.

Two properties of the script that matter when re-running it:

- **It is billable and needs BOTH:** a real, authenticated `claude` CLI for the invoking OS user (the `claude-sdk` arm), and a provider key resolvable for `PROVIDER_KEY_REF` (default `opencode-go`, read from the single-user dev home; override with `PROVIDER_KEY_FILE`) for the `openai-api` arm. Two small real turns total. It is a manual gate, never a CI job.
- **It does not re-verify `settingSources: []`.** That pin is unchanged by Phase A (`sdk-engine.ts` is not touched) and is already covered deterministically by `sdk-engine.test.ts`'s unit-level `expect(options.settingSources).toEqual([])`, which runs in the ordinary test suite -- intercepting the same fact from a live billed SDK session here would be strictly less reliable, not more, for a file this class of PR does not modify.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Labeled-Issue Webhook Routing Shipping-Path E2E

PRs touching `packages/server/src/routes/webhooks.ts`, `packages/server/src/services/inbound/**` (in particular `handlers.ts`'s `canDeliverToAgentWorker` / `AgentWorkerHandler`, whose delivery now goes through `SessionManager.deliverWorkerNotification` -- Issue [#1739](https://github.com/ms2sato/agent-console/issues/1739)), `RepositoryManager`'s `addOrchestratorSession` / `removeOrchestratorSession` / `getRepositorySlug` in `packages/server/src/services/repository-manager.ts`, or the `set_orchestrator_session` / `clear_orchestrator_session` MCP tools in `packages/server/src/mcp/mcp-server.ts`, must run `bun scripts/smoke/check-webhook-issue-label-routing.ts` locally before pushing. This is the shipping-path E2E for routing a labeled-Issue GitHub webhook to a repository's designated Orchestrator session (Issue [#1643](https://github.com/ms2sato/agent-console/issues/1643) PR-1): it boots a disposable server instance as a REAL CHILD PROCESS (not an in-process `import()`, unlike this file's other server-story smokes -- see the script's own header comment for why: proving the "non-matching label" path logs its non-match message requires capturing real pino output, and pino's default destination writes directly to fd 1, bypassing any in-process `process.stdout.write` interception), drives a real signed `POST /webhooks/github` through the real async job queue, `createInboundEventJobHandler`, and `resolveTargets`, against a real disposable git repository with three real `git worktree add` worktrees (sessions D, D2 and T) and a real `quick` delegate session (child of D).

**FREE AND DETERMINISTIC** -- no `claude` CLI turn, no provider key, no LLM call anywhere in this path (the two `claude-code-builtin` PTYs the script spawns for D and T are interactive shells with no prompt ever sent). The exception here is cost, not gate placement, same convention as `check-restore-failure-declaration.ts`'s header.

**Four scenarios in one run, and the controls are the point.** (1) POSITIVE CONTROL: `issues` `closed` -- unaffected by this PR, still fans out via the OLD per-session loop to both D and T (never the `quick` delegate), confirmed via a direct SQLite read of `inbound_event_notifications`. (2) NEGATIVE CONTROL: `issues` `labeled` with a non-matching label -- zero new notification rows, plus the server's own captured stdout contains `resolveIssueLabeledTargets`'s exact non-match log line (`"issue:labeled event did not match repository's configured trigger labels"` in `resolve-targets.ts`), so the negative result is attributed to the code path actually running rather than to silence indistinguishable from a broken harness (`workflow.md` sub-pattern 9/5). (3) POSITIVE: `issues` `labeled` with a matching label -- deliberately cased differently than the repository's configured `issueTriggerLabels` value, exercising `matchesAnyTriggerLabel`'s case-insensitive compare rather than a naive `===` -- routes exclusively to the repository's DESIGNATED SET, D and D2 (Issue [#1716](https://github.com/ms2sato/agent-console/issues/1716): both designated through real `set_orchestrator_session` calls, the second asserted to add D2 without displacing D), confirmed by reading D's AND D2's real worker output files for the `[inbound:issue:labeled]` PTY-notification tag; T and the delegate must NOT receive it. (4) POSITIVE: `issues` `opened` with a full label set (not `labeled`'s single-added-label shape) containing the matching label among others, exercising `parseIssueOpened`'s distinct matching path, same D-and-D2-not-T assertion shape. (4b) POSITIVE, hibernated-designated-is-skipped (#1716 §5): D2 is PAUSED via `POST /:id/pause` and the scenario 3 shape is posted again -- only D receives, D2's and T's output files are unchanged, the server's captured stdout contains `resolveIssueLabeledTargets`'s exact per-session skip reason (`"... a designated orchestrator session is not a live session"`) naming D2, and `GET /api/repositories/:id` still lists D2 in `orchestratorSessionIds` -- routing skipped it, nothing auto-removed it. D2 stays paused for scenarios 5-7, and scenario 5 additionally asserts the paused D2 does not receive the fallback either (same per-session exclusion on the fallback path).

**Scenario 4c and the scenario-5 embedded assertion (Issue [#1739](https://github.com/ms2sato/agent-console/issues/1739)) cover an EMBEDDED-designated recipient, and are the shipping-path E2E for routing `AgentWorkerHandler` through `SessionManager.deliverWorkerNotification`.** After 4b, the smoke hosts a stub OpenAI-compatible provider in its own process (`Bun.serve`, port 0; every `POST /v1/chat/completions` answered with a scripted final assistant message, no tool calls, every request body recorded), creates an `openai-api` definition through the real `POST /api/embedded-agents` with `provider.baseUrl` pointed at the stub and a fake `apiKeyRef` resolved from a `provider-keys.json` written into the disposable home, creates a fourth worktree session E (`smoke-branch-e`) whose initial worker is that embedded agent (`embeddedAgentId`; E has NO PTY `agent` worker), and designates E through a real `set_orchestrator_session` call, asserting the set reads exactly {D, D2, E}. (4c) `issue:labeled` with the matching label: D receives the PTY tag as in scenario 3; E's worker -- created dormant and never activated by the smoke -- is woken by the event itself (`sendSystemNotification` -> `deliverUserTurn` -> `ensureDeliverable`), and E's NDJSON output file gains, after a baseline captured before the POST and read only after the turn settles (the stub answered AND a `state: 'idle'` event landed), exactly one `user-message` event with `notification.kind === 'inbound-event'` carrying `[inbound:issue:labeled]` and the same `type=` / `intent=` fields as the PTY text; the stub saw exactly ONE chat request since the POST and its messages carry that tag text (the activation-on-delivery proof); T does not receive; and, after a positive control that the stdout pipe has caught up (the same routing pass's 'not a live session' record naming the still-paused D2), the server's stdout since the POST does NOT contain the 'no agent worker to deliver to' skip reason naming E. (5-E) The main-push fallback additionally asserts E received `[inbound:ci:completed]` the same way, with exactly one more stub request. Polarity (Q12, measured on unmodified `main` before the change): every E assertion FAILS and the skip record IS present naming E. Teardown deletes E's session through the real route before the child server is killed, stops the stub only after the server has exited, and prints the stub's port and request log when any assertion failed. The remaining scenarios are unchanged: 6 and 7 still assert "T receives neither", and 4c was inserted before scenario 5 so scenario 7 stays the LAST webhook poster (its "most recent job" query depends on that).

**Two more scenarios extend this smoke for the designated-session fallback (Issue [#1661](https://github.com/ms2sato/agent-console/issues/1661)), covering the per-session-loop path's "resolves to nobody or a dead session" gap that `issue:labeled` routing does not share.** (5) POSITIVE, main-push fallback: a `workflow_run` `completed`/`success` event on `head_branch: 'main'` matches zero registered sessions (neither D's nor T's worktreeId) -- `resolveTargets`'s new fallback routes it to D, confirmed by reading D's real worker output file for the `[inbound:ci:completed]` tag; T must not receive it. `head_sha` is omitted from the payload so `job-handler.ts`'s `ciCompletionChecker` gate (active only when `commitSha` is present) never runs. (6) POSITIVE, dead-parent fallback: a third worktree session O is created with `parentSessionId` pointing at a real session P, which is then paused via `POST /:id/pause` -- pausing removes P from the live session manager (`getAllSessions()`) while preserving its DB row, exactly matching `resolveTargets`'s "parent absent from getSessions()" shape. The same `workflow_run` shape as scenario 5, branch-matching O, routes to BOTH O (direct match) and D (fallback, since O's parent P is no longer live); T must receive neither. Scenario 6 deliberately does NOT use a literal syntactically-valid-but-never-persisted session id for the "dead" parent -- that construction was tried first and found to trip a separate `job-handler.ts` defect (no per-target failure isolation: a `createPendingNotification` insert for a target whose `sessionId` has no corresponding `sessions` row threw an uncaught `FOREIGN KEY constraint failed`, crashing and retrying the WHOLE job until it stalled, so the fallback target -- appended last in the array -- never got processed). **That defect has since been fixed (Issue [#1677](https://github.com/ms2sato/agent-console/issues/1677), per-target failure isolation in `job-handler.ts`) and is now exercised directly by scenario 7 below**; the paused-session construction here continues to exercise the same `resolveTargets` code path for the DIFFERENT "parent's DB row survives, but the parent is absent from the live session manager" shape.

**A seventh scenario (Issue [#1677](https://github.com/ms2sato/agent-console/issues/1677)) tests the genuinely-deleted-parent shape directly, plus proves the fix stops job-level retry.** (7) POSITIVE, deleted-parent fallback + no job retry: a fourth worktree session Q is created with `parentSessionId` pointing at a real session R, which is then genuinely DELETED via `DELETE /api/sessions/:id` -- unlike scenario 6's pause, this removes R's row from the `sessions` table entirely, reproducing the exact FOREIGN KEY-constraint shape scenario 6's comment describes. The same `workflow_run` shape as scenarios 5-6, branch-matching Q, routes to BOTH Q (direct match) and D (fallback, since Q's parent R no longer exists); T must receive neither. Beyond the routing assertions, this scenario opens a read-only handle on the disposable server's own `jobs` table and asserts the job that processed this webhook has `status = 'completed'` and `attempts = 0` -- direct proof that the dangling target no longer crashes and job-level-retries the whole job.

**A real, environment-specific wrinkle this script had to work around: the interactive `claude` CLI's own trust dialog.** A brand-new working directory (every fresh `git worktree add` path) trips the CLI's "Do you trust the files in this folder?" TUI prompt at PTY startup, which blocks forever with no automatic resolution -- unlike `claude-sdk` embedded-agent workers (which set the SDK's `permissionMode` and never show this prompt), no other smoke in this file exercises a real terminal-agent PTY, so this is the first place it surfaces. The script bypasses it by seeding an ISOLATED `CLAUDE_CONFIG_DIR` (a copy of the real OS user's own `~/.claude.json`, read-only, with `projects` entries added for the worktree paths -- never a mutation of the real file, so this smoke has no persistent side effect on the host) before spawning the disposable server; `CLAUDE_CONFIG_DIR` is not in `packages/server/src/services/env-filter.ts`'s blocked/protected lists, so it passes through to the spawned PTY unchanged. A from-scratch (non-copied) config was tried first and rejected: it lacks `hasCompletedOnboarding` and triggers the CLI's full first-run onboarding wizard instead, an equally-blocking prompt.

This is a real-process regression gate for the shipping webhook->job-queue->handler chain, not a sibling-test requirement, so it is not part of the `preflight-check.js` coverage patterns above. It is a manual gate, never a CI job (same as this file's other real-child-process smokes).

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Unprivileged (no tier-2/3/4 residue; any machine with bun) — free and deterministic, no `claude` CLI, no provider key.

## Additional Verification: Memory Layer Cross-Session Recall E2E, both engines

PRs touching the memory layer's READ half (`loadMemoryLayer` in `packages/embedded-agent/src/system-prompt.ts`), its WRITE half (`BuiltinToolContext.memoryRoot` in `packages/embedded-agent/src/tools/types.ts` and its three forwarders `read.ts` / `write.ts` / `edit.ts`), the memory path writer (`SessionDataPathResolver.getMemoryDir` in `packages/server/src/lib/session-data-path-resolver.ts`), the activation-time directory creation + verification (`packages/server/src/lib/memory-dir.ts`, or `EmbeddedAgentWorkerService`'s `ensureMemoryDirFn` dep in `packages/server/src/services/embedded-agent-worker-service.ts`), or the definition-deletion job (`CLEANUP_DEFINITION_MEMORY` in `packages/server/src/jobs/handlers.ts` and `buildDefinitionMemoryCleanupTargets` in `packages/server/src/lib/session-data-path.ts`) must run `bun scripts/smoke/check-embedded-agent-memory-layer.ts` locally before pushing. This is the shipping-path E2E for the memory layer (epic [#1636](https://github.com/ms2sato/agent-console/issues/1636) Phase 2; spec `docs/design/embedded-agent-worker.md` "Verification plan for PR-3"): it boots a disposable server instance with a real `AppContext` and a real `/mcp` on a real port, registers two real disposable git repositories (one with a fake `origin` remote, so both slug shapes the Session Data Path design produces are exercised), and per engine drives four real embedded-agent workers in four real worktree sessions -- A plants, B recalls, C and D are the controls.

**Why "recall after a restart" is the wrong instrument, and what this measures instead.** A restarted worker remembers a nonce from its own transcript (SDK resume on `claude-sdk`, NDJSON reconstruction on `openai-api`), so a same-worker recall passes identically with the layer removed. The property this layer exists for is continuity across SESSIONS: B is a fresh worker in a fresh session on the same (definition, repository) key. Step 1 (A, S1, D1) reads a nonce file in `cwd` with a tool (the tool-turn rule below) and is told to record it in memory; the script then asserts DETERMINISTICALLY on disk, before any recall, that a topic file under the server-owned `memoryDir` contains the nonce, that a `MEMORY.md` line's link target is that file, and that the nonce is absent from `MEMORY.md` itself -- so a later recall cannot be satisfied by the index alone. The cwd nonce file is then deleted (the second route closed) and every recall turn is pinned to have emitted no `tool-call` against it. Step 3 (B, S2, same repository, D1): the answer must contain the nonce AND B's turn must contain a `tool-call` whose path is under `memoryDir` -- the on-demand topic read asserted as a SHAPE from B's persisted NDJSON, scoped to B's turn by baseline index, never inferred from the answer text; absence assertions are snapshotted after B's `idle`. Steps 4-5: C (S3, same repository, a different definition D2) and D (S4, D1, a different repository) are asked the identical question and must answer `UNKNOWN`. The key has two halves; C and D each falsify one, so a pass cannot be explained by a guessable question or by a memory that is secretly global. Both controls are REQUIRED -- a run missing either is exit 2, not a pass. Every positive arm also pins the served `init` frame's `context.memoryDir` against the resolver's path.

**It has a polarity mode.** `bun scripts/smoke/check-embedded-agent-memory-layer.ts -- --expect-no-memory` passes `async () => undefined` as `createTestContext`'s `ensureMemoryDirFn` override (the layer's single seam, `EmbeddedAgentWorkerServiceDeps.ensureMemoryDirFn`; source untouched, no runtime reassignment of any private field), so `init` carries no `memoryDir` and the loader renders no layer at any call site. It runs the plant and B's recall only (no positive claim, so no controls) and asserts the absence on the wire (every captured `init` frame lacks `memoryDir`), on disk (no `<base>/memory` directory exists after B settles), and in the answer (B says `UNKNOWN`). A recall under the flag is reported as a polarity FAILURE, never tolerated -- run it FIRST, before the positive arms, to confirm the apparatus reaches the defect, per `workflow.md`'s "A check's existence is not its detection power".

**Q13 record (the script's header carries the same list).** Four proxies, all upstream of and outside the chain under test, nothing else substituted: (a) the nonce is delivered through a file in `cwd` rather than in prose -- it changes how the model arrives at the fact, never what memory does with it; (b) in polarity mode the overridden `ensureMemoryDirFn` stands in for "the layer is absent" -- it substitutes the decision to send `memoryDir`, which sits before the chain; (c) the `init` frame is read through a write-tap on the stdin sink returned by `createTestContext`'s existing `spawnAsUserFn` override (the real `spawnAsUser` runs; the tap only records); (d) on the `claude-sdk` arm the second definition D2 has no public creation path (`createEmbeddedAgent` hardcodes `engine: 'openai-api'`), so it is persisted as a real row through the real repository in a first disposable context and loaded by the real boot path (`EmbeddedAgentManager.initialize`) of the context the smoke uses. **The disposable home is single-user** (`AUTH_MODE` unset, so the `0700` contract): the multi-user `2775` contract is NOT exercised here -- it is verified at tier 3 by the elevation smoke and by the real-fs test in `memory-dir.test.ts`, which now runs for real in CI's second `bun test` invocation (`packages/server/package.json`, Issue [#1699](https://github.com/ms2sato/agent-console/issues/1699)), and this smoke must NOT be switched to multi-user without `createDisposableMultiUserHome` (Issue [#1713](https://github.com/ms2sato/agent-console/issues/1713)).

Two properties of the script that matter when re-running it:

- **It is billable and needs BOTH** a real, authenticated `claude` CLI for the invoking OS user (the `claude-sdk` arm) and a provider key resolvable for `PROVIDER_KEY_REF` (default `opencode-go`, read from the single-user dev home; override with `PROVIDER_KEY_FILE`) for the `openai-api` arm. Roughly four small turns per engine (plant, recall, two controls) plus two per engine for the polarity run. It is a manual gate, never a CI job.
- **`--engine openai-api|claude-sdk|both` selects the arm** (default `both`); a single-engine run needs only that engine's credential.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: SDK Declared-MCP and `Task` Premise Probe

Before implementing epic [#1636](https://github.com/ms2sato/agent-console/issues/1636) Phase 5 (declared external MCP servers + `Task` on the `claude-sdk` arm; design in `docs/design/embedded-agent-sdk-engine.md` section 4.5, Issue [#1726](https://github.com/ms2sato/agent-console/issues/1726)), before changing `strictMcpConfig` / the `mcpServers` composition / the `mcp__` exemption in `handleSystemInit` in `packages/embedded-agent/src/sdk-engine.ts`, or before relying on any claim about what a subagent inherits under this engine's `tools:` allowlist, run `bun scripts/smoke/probe-sdk-declared-mcp-and-task.ts` against a real, authenticated `claude` CLI session. This is the Task 0 gate probe for that phase. Every arm builds the exact `buildOptions()` battery, varies ONE thing, and reads the SDK's own `system:init` (`tools`, `mcp_servers`) plus `Query.mcpServerStatus()` after the turn plus `PreToolUse` / `PostToolUse` / `SubagentStart` hook firings -- never the model's prose alone.

**Four arms (`--p0` / `--p1` / `--p2` / `--p3`; omitted = all four, in order).** `--p0` BASELINE: today's battery unchanged against a config dir seeded with the operator's real user-scope `mcpServers` block -- any `mcp_servers` entry or `mcp__` tool outside the reserved `agent-console` / `console` pair is a PRE-EXISTING LEAK (its own Issue, independent of Phase 5); a same-arm positive control with `settingSources` omitted must SEE the seeded server, or the arm is INCONCLUSIVE rather than "no leak". `--p1` STRICT: `strictMcpConfig: true` -- names exactly the reserved pair, `console` connected with `mcp__console__Compact` / `mcp__console__TodoWrite` present (the regression control), `agent-console` recorded by name. `--p2` DECLARED: P1 plus the host's own `chrome-devtools` entry declared verbatim from `~/.claude.json` (one real `list_pages` call observed through the hook) and a `missing-command` negative control (status `failed`, no tools, turn completes) compared to P1 by NAME. `--p3` TASK, two halves: `tools + ['Task', 'Agent']` with no `agents` (both names, because this CLI silently drops an unrecognized `tools:` entry and `sdk.d.ts` uses both names -- the honored one is itself a finding), then the same plus `agents: { probe: { tools: ['Read'] } }`; the child's tools are read from `agent_id`-bearing `PreToolUse` firings, and a child `Bash` call while the parent allowlist has none is a `STOP:` line.

**Two recorded proxies (pre-pr-completeness Q13), both upstream of and outside the chain under test.** (a) The isolated `CLAUDE_CONFIG_DIR` is seeded with ONLY the `mcpServers` block copied from the operator's real `~/.claude.json`, because an unseeded isolated dir has no user-scope config and P0 would read "no leak" for the wrong reason; verified free of cost that `CLAUDE_CONFIG_DIR=<seeded> claude mcp get chrome-devtools` reports the User-scope entry. (b) Production's `agent-console` HTTP MCP server is a console dial-back; the probe stands up a minimal real Streamable-HTTP MCP server in its own process and points the same `type: 'http'` / bearer-header / `alwaysLoad: true` declaration at it. What stays production-real: the declaration shapes, `strictMcpConfig`, the SDK's own config discovery and connection handling.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user, plus a User-scope `chrome-devtools` MCP server in `~/.claude.json` (the P0 seed and the P2 declared server; `claude mcp get chrome-devtools` must report it). About 8 small turns across the four arms. It is a manual gate, never a CI job.
- **Three exit codes, and this is a measurement script.** `0` = every selected arm produced a definite reading (a P0 leak or a P3 containment escape is a MEASUREMENT, printed as a `STOP:` line, and still exits 0); `1` = INCONCLUSIVE for at least one selected arm (a control failed, a turn did not settle); `2` = HARNESS (bad arguments, unverified isolation, the host precondition missing). **A P0 leak halts the run before P2/P3** unless `--continue-after-leak` is passed -- a live leak changes P2's negative-control design and must be reported first. The mapping and every per-arm classifier are pinned by `scripts/smoke/__tests__/probe-sdk-declared-mcp-and-task.test.ts`, which imports them directly and never runs the billed measurement.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: SDK `settingSources`/`managedSettings` Wall Premise Probe

Before implementing epic [#1636](https://github.com/ms2sato/agent-console/issues/1636) Phase 5's `settingSources: ['user']` design (owner directive, 2026-09-20; design in `docs/design/embedded-agent-sdk-engine.md` section 4.5), or before relying on any claim about what `Settings.enabledMcpjsonServers` / `disabledMcpjsonServers` / `enableAllProjectMcpServers`, `Options.managedSettings` vs `Options.settings` as carriers for `allowedMcpServers` / `deniedMcpServers`, `Settings.claudeMdExcludes`, `Settings.disableClaudeAiConnectors`, or `Settings.disableAllHooks` actually do on a real `claude-sdk` session, run `bun scripts/smoke/probe-sdk-mcp-settings-sources.ts` against a real, authenticated `claude` CLI session. This is the Task 0 measurement for that directive -- it reuses its sibling `probe-sdk-declared-mcp-and-task.ts`'s `startAgentConsoleStandIn` and `readUserScopeMcpServers` exports (that probe's own four arms are unchanged) and shares its fixture, `scripts/smoke/fixtures/stdio-echo-mcp-server.ts` -- a stdio MCP server that touches a `--canary <path>` file at startup (before the transport ever connects) and serves one `probe_echo` tool reporting its own `argv` and one named env var, so "did the native loader even attempt to spawn this server" is read from disk, independent of whatever `system:init.mcp_servers[].status` reports.

**Seven core arms (`--armA` / `--armC` / `--armB` / `--armF` / `--armE` / `--armD` / `--armG`; omitted = all seven, in owner-directed order) plus addenda the live measurement run itself required (`--armA3`, `--armC1b`, `--armC1d`, `--armC1e`, `--armC1c`, `--armG2`, `--armG3` -- explicit-only, never in the default set).** Operationally this probe was run across MANY invocations, not one: `--armA` alone first, its result reported before anything else started; the addenda were dispatched by the Orchestrator/Architect live, in response to findings each prior arm surfaced (see Issue [#1781](https://github.com/ms2sato/agent-console/issues/1781)'s PR for the full transcript and per-arm tables).

- `--armA` -- `settingSources: ['user']`, strict OFF, plus `A+` (omitted, positive control), `A-` (`[]`, isolation), `A2` (`['user']` + `strictMcpConfig: true`). 4 sessions. **Measured finding: local-scope MCP (L) does NOT start under `['user']` alone** -- `A+` proves L's config is reachable, so the gap is `settingSources` scope, not the fixture.
- `--armA3` (addendum) -- `settingSources: ['user','local']`. 1 session. **Clean confirmation**: U+L start, X/Y do not, CLAUDE.md/agent shape unchanged from plain A -- the exact knob design II needs to cover User+Local without leaking project scope.
- `--armC` -- `['user','project']`, strict OFF (baseline). C1/C2/C3 (`allowedMcpServers` exact-argv / mutated / empty) and C5 (`deniedMcpServers`), each via BOTH `managedSettings` and `settings` = 9 sessions. **Measured finding, refuting the AC's cited doc premise: `managedSettings.allowedMcpServers` is NOT dropped -- it is fully and universally enforced**, blocking even the reserved `agent-console` HTTP server and all four claude.ai connectors whenever set with ANY content (C1/C2/C3 collapsed to an identical "everything blocked" reading in BOTH carriers). C5's `deniedMcpServers` survives as documented.
- `--armC1b` (addendum, diagnostic) -- a NAME-ONLY allow entry (no `serverCommand`), both carriers. 2 sessions. Resolves C1/C2/C3's collapsed reading: X IS admitted by name, proving the field works and the compound `serverName`+`serverCommand` entry's argv comparison was the broken piece, not the field itself.
- `--armC1d` / `--armC1e` (addendum, Architect's exact shape) -- one `settings.allowedMcpServers` entry naming `agent-console`, `console`, X, and a claude.ai connector (label form, then slug form) by `serverName` alone. 2 sessions. **Load-bearing finding**: naming the reserved pair re-admits `agent-console`; connectors are NEVER re-admittable by any naming form. **Refined by C1c's byproduct** (below): `console`, an in-process SDK server with no command/url, was never actually gated by `allowedMcpServers` at all -- it connects whether named or not, so only `agent-console` needs explicit naming.
- `--armC1c` (addendum, exact-argv re-test) -- C1's shape re-run with `command` as an absolute path (`process.execPath`), plus C2' (one arg mutated), settings carrier only. 2 sessions (capped, per the Architect's own fallback). **Both still block X** -- exact-argv `serverCommand` matching is unusable on SDK 0.3.238, confirmed NOT a path-representation mismatch via a free, non-billed `claude mcp get` check showing the CLI's recorded command/args byte-identical to what was passed.
- `--armB` -- `['user','project']`, strict OFF. B1 `disabledMcpjsonServers` (spawn-canary confirms X never exec'd, not merely disconnected), B2 `enabledMcpjsonServers` (record, not a gate -- the un-listed server still starts), B3 `enableAllProjectMcpServers: false` (no observed effect in this shape). 3 sessions.
- `--armF` -- `['user','project']` + `Settings.claudeMdExcludes` (design III's feasibility input) via both carriers, plus a no-excludes control and a narrow, absolute-path-only F2 variant (added live; project file only, not the broad `**/CLAUDE.md` glob). 4 sessions. **Findings**: the broad exclude via `settings` suppresses ALL THREE CLAUDE.md/rules canaries (including user-level -- the glob is absolute-path-matched); the SAME exclude via `managedSettings` suppresses NONE (dropped -- the opposite carrier behavior from `allowedMcpServers`); the narrow F2 exclude (naming only the project's own file) suppresses project only, leaving user + rules intact -- exactly the scoped knob design II needs.
- `--armE` -- `['project']` + a repo-level `PreToolUse` hook (matcher `Read`) that touches a hook-canary file; control fires it via a real `Read` call, `disableAllHooks: true` suppresses it. 2 sessions. Clean, matches doc.
- `--armD` -- `disableClaudeAiConnectors: true`; reuses arm C's baseline connector reading when run in the same invocation, or runs its own control session standalone. 1-2 sessions. Clean: all 4 connectors gone, everything else unaffected.
- `--armG` -- an explicit `Options.mcpServers` entry whose `args` contains the literal string `${PROBE_VAR}`, versus a DEDICATED `.mcp.json`-declared server with the identical shape (not a reused sibling server -- an earlier version's control reused X's plain declaration, which never carried the placeholder argument, and was discarded). 2 sessions. **Finding: `${PROBE_VAR}` is passed through LITERALLY in `args`, never expanded, on both declaration paths** -- contradicts the AC's "docs say expanded" premise for `args` specifically.
- `--armG2` / `--armG3` (addenda, Orchestrator's follow-up) -- the SAME `${VAR}`-expansion question for `env:`, `headers:`, and `url:` fields instead of `args:`, once via explicit `Options.mcpServers` (G2) and once via `.mcp.json` (G3). 1 session each. **Finding, a per-field asymmetry**: all three of `env:`/`headers:`/`url:` DO expand `${VAR}` (on both declaration paths), while `args:` does not.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user, plus the same User-scope `chrome-devtools` precondition its sibling probe needs (this probe calls `readUserScopeMcpServers()` too, though only as a precondition check -- it seeds none of its OWN servers from it). `--max-usd <n>` (default 3) halts the run before starting a session that would push the running total over budget; a halted run can never report MEASURED. It is a manual gate, never a CI job.
- **Three exit codes, and this is a measurement script**, same convention as its sibling: `0` = every selected arm produced a definite reading (a containment deviation is a MEASUREMENT, printed as a `STOP:` line, and still exits 0); `1` = INCONCLUSIVE for at least one selected arm (a control failed, a turn did not settle); `2` = HARNESS. The mapping and every per-arm classifier are pinned by `scripts/smoke/__tests__/probe-sdk-mcp-settings-sources.test.ts`, which imports them directly and never runs the billed measurement.

**MUST NOT, beyond the general probe discipline above:** proceed to `--armC` before `--armA`'s result has been reported; change anything in the sibling probe's own four arms (only its two named exports were added); write to the operator's real config dir (`isolateClaudeConfigDir` + `verifyIsolation`, same as every sibling probe); call any connector's tool.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Phase 5 PR-2 MCP Discovery Premise Probe (P-a/P-b)

Before implementing epic [#1636](https://github.com/ms2sato/agent-console/issues/1636) Phase 5 PR-2 (Issue [#1785](https://github.com/ms2sato/agent-console/issues/1785); the `claude-sdk` engine's own MCP-server discovery/permission machinery, design in `docs/design/embedded-agent-sdk-engine.md` §4.5 design II), or before relying on any claim about whether `Query.setMcpServers` can add a server to an already-running session or whether `Options.agents` can declare a subagent programmatically on this engine, run `bun scripts/smoke/probe-sdk-phase5-pr2-premises.ts` against a real, authenticated `claude` CLI session. This measures two premises PR-2's design depends on that neither of its two MCP-probing siblings (`probe-sdk-declared-mcp-and-task.ts`, `probe-sdk-mcp-settings-sources.ts`) measured: those siblings probe a STATIC `Options` battery built at session construction; PR-2's discovery-then-approve flow needs a server added to a session that is ALREADY running, and a subagent constrained programmatically rather than via a `.claude/agents/*.md` file the SDK may or may not load natively under a given `settingSources`.

**P-a: live add via `Query.setMcpServers`.** One session, built with design II's battery (`settingSources: ['user', 'local']`, no `strictMcpConfig`, `bypassPermissions`, the reserved `agent-console`/`console` pair only), spends a baseline turn, then calls `setMcpServers` re-passing the full reserved pair (the SDK doc: this call REPLACES the current dynamic set) plus a new stdio server (the shared `stdio-echo-mcp-server.ts` fixture) and a NEGATIVE CONTROL server whose command does not exist. Positive-control pass conditions: the fixture's own spawn canary exists (proof the OS process was actually exec'd), `mcpServerStatus()` reports the new server `connected`, and a real tool call was observed via `PostToolUse`. Negative-control pass conditions: the missing-command server appears in `McpSetServersResult.errors` or as `failed` via `mcpServerStatus()`, no tool from it ever appears, and the SAME turn still settled normally. Two RECORD-only readings (not gated, printed for the design write-up): whether the reserved pair's own status survives the `setMcpServers` re-pass, and whether re-passing the SAME in-process `console` instance a second time is accepted cleanly or reported as a duplicate registration.

**P-b: agents through `Options.agents`.** A scratch git repo carries a `.claude/agents/probe-agent.md` file (a "FILE VERSION" marker in its prompt). SUBJECT: the same battery plus `Options.agents` declaring `probe-agent` with an "OPTION VERSION" marker and its own nonce, plus `'Task'` added to the parent's allowlist; one turn delegates to `subagent_type: 'probe-agent'`. Pass conditions: `system:init.agents` names `probe-agent` EXACTLY ONCE (not twice -- the file version must not ALSO load natively alongside the option version), a `SubagentStart` firing is observed, and the relayed answer carries the OPTION nonce and says OPTION rather than FILE. CONTROL: the identical repo and battery with `Options.agents` omitted; pass condition is that `system:init.agents` does NOT name `probe-agent` at all (re-pins PS10 for this specific battery). A SUBJECT failure is a STOP finding per the design AC ("STOP and report to the Orchestrator"), still exit 0 -- a STOP is a definite reading, not an inconclusive one.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user -- the `claude-sdk` engine runs as the executing user and uses that user's own authentication, so there is no provider key to configure. Roughly 4 small turns per full run (P-a: baseline + tool-call; P-b: subject + control). It is a manual gate, never a CI job.
- **`--p-a` / `--p-b` select an arm**; omitted, the default is both, in order (P-a first, then P-b). Exit codes follow the same convention as both sibling MCP probes: `0` = every selected arm produced a definite reading (a STOP finding is a MEASUREMENT, printed as a `STOP:` line, and still exits 0); `1` = INCONCLUSIVE for at least one selected arm (a control failed, a turn did not settle); `2` = HARNESS. The mapping and every classifier are pinned by `scripts/smoke/__tests__/probe-sdk-phase5-pr2-premises.test.ts`, which imports them directly and never runs the billed measurement.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Phase 5 PR-2 MCP Discovery Premise Probe (P-c, `${VAR}` substitution through the production spawn path)

PRs touching `applyArgSubstitution` / `discoverProjectMcpServers` in `packages/embedded-agent/src/mcp-discovery.ts`, the `${VAR}`-substitution call site in `packages/embedded-agent/src/main.ts` (around `applyArgSubstitution(entry.name, config.args, process.env)`), or `EmbeddedAgentWorkerService.activate`'s `allowedProjectMcpServers` composition (`listAllowedProjectMcpServerPairs` in `packages/server/src/lib/mcp-server-permissions.ts`), must run `bun scripts/smoke/probe-sdk-phase5-pr2-pc.ts` against a real, authenticated `claude` CLI session. This is P-c, the THIRD and last premise probe for epic [#1636](https://github.com/ms2sato/agent-console/issues/1636) Phase 5 PR-2 (Issue [#1785](https://github.com/ms2sato/agent-console/issues/1785)), and unlike its siblings P-a/P-b (`probe-sdk-phase5-pr2-premises.ts`, `ProbeSession`-harness, direct `Query` construction), it drives the PRODUCTION shipping path itself: a real `claude-sdk` embedded-agent worker spawned by the real `EmbeddedAgentWorkerService.activate`, through a real disposable `AppContext` on a real `/api` + `/mcp` port -- the `check-embedded-agent-idle-eviction.ts` pattern, not the `ProbeSession` pattern. It measures whether `${VAR}` placeholders in an ALLOWED `.mcp.json` server's `env` and `args` are actually resolved end to end through that real subprocess boundary: a real repository row (real `git init`, no real worktree needed), a real `.mcp.json` written into the session's `locationPath`, its hash computed via the REAL `discoverProjectMcpServers` (never hand-computed), and two rows seeded as `allow` through the REAL `McpServerPermissionRepository`.

**Two fixture servers, not one, because the shared `stdio-echo-mcp-server.ts` fixture only echoes ONE named env var per instance.** `probe-pc-fixture` references `${PROBE_VAR}` (SET in the server process's own environment before activation) in both its `args` and `env`; `probe-pc-fixture-unset` references `${PROBE_UNSET}` (the Issue's own "Control", deliberately never set) the same way. Both channels are read from ONE tool call's response per fixture -- the fixture's `probe_echo` tool reports its own `process.argv` (proves what the LOADER's `applyArgSubstitution` put into `args`) and the named env var's value (proves what the CLI's OWN native expansion put into `env`) in a single JSON payload, read directly off the worker's persisted `tool-result` event (a double JSON decode: the MCP content-block array, then the fixture's own JSON text inside it) -- never inferred from the model's prose.

**TWO ARMS, ONE PROCESS INVOCATION EACH (Issue #1799).** `bun scripts/smoke/probe-sdk-phase5-pr2-pc.ts` (no arguments) runs the NON-ELEVATED branch above, billable. `bun scripts/smoke/probe-sdk-phase5-pr2-pc.ts --elevated <target-user>` runs the ELEVATED branch, and is FREE and TURN-FREE: it activates a real `claude-sdk` worker through the real `spawnAsUser` as `<target-user>`, with the SAME `.mcp.json`/permission-seeding shape as the non-elevated branch, but NEVER sends a user message. Its observable is each fixture's own `--spawn-report` file (`fixtures/stdio-echo-mcp-server.ts`'s job 4, added in the same PR), written at spawn time, before any model turn could exist -- so the arm needs the CLI to START its declared MCP servers, not to answer a prompt. This is what makes it runnable in a container with no `claude` login at all, closing the exact gap this section's original filing raised (`verify-multiuser-docker.sh`'s "no `claude` login inside the container, therefore no billable smoke" rule is respected literally: no login, no turn). The two arms never run in the same process invocation.

**The non-elevated arm isolates `CLAUDE_CONFIG_DIR` from the operator's real `~/.claude.json`** via `isolateClaudeConfigDir` plus a `{}` `.claude.json` -- the same construction `check-embedded-agent-project-mcp-permission.ts`'s negative control (c) uses -- and asserts no `mcp-servers-discovered` event ever reports a `user` / `local`-scope row, and `userLocalNamesUnavailable` is never set (Issue [#1813](https://github.com/ms2sato/agent-console/issues/1813): before this isolation existed, the CLI's own user-scope MCP servers leaked into and were spawned by this measurement as a side effect). **This isolation covers only the `user`/`local` FILE channel; claude.ai connectors are account-credential-bound (not `.claude.json`-bound), stay accepted as TUI-parity capability surface per `docs/design/embedded-agent-sdk-engine.md` section 4.1, and are printed as a RECORDED, non-gated reading instead (Architect ruling, Issue #1813).** The elevated arm needs no such isolation -- it is isolated by construction (the target user carries no credentials of any kind, per this section's own MUST NOT below).

**Measured 2026-09-21 (non-elevated branch):** SET case -- CLI-native `env` expansion delivered the real value; the loader's `args` substitution delivered the same real value. UNSET case (Control) -- the CLI left `env` as the literal, unexpanded placeholder string (one of the two outcomes the Issue's own AC anticipated, "empty/literal"); the loader left `args` literal per `applyArgSubstitution`'s documented unset-with-no-default behavior; the loader's stderr warning (`"...is unset and has no default; leaving ... literal in args"`) was directly observed in the subprocess's piped stderr, captured via `LOG_LEVEL=debug`. The same run also asserts a same-run positive control: the spawn report (item 1's addition) is byte-equal to the tool-result reading for BOTH fixtures -- a mismatch is a harness failure, not a finding, and this equality is what licenses reading the elevated arm from the spawn report alone.

**Measured 2026-09-22 (elevated arm, item 4's free gate).** Run inside the tier-2 verification container (`scripts/verify-multiuser-docker.sh --keep`), as a real second OS user (`alice`) carrying NO credentials of any kind (`{}` `.claude.json` only, no `.claude/` directory, no `claude` binary on PATH), through the real `spawnAsUser` elevation (`runAsUser`/`spawnAsUser` logs confirm `elevated: true`, not a same-user bypass). OUTCOME (i): both declared MCP servers started WITHOUT a login. Both channels (env and args) left `${PROBE_VAR}` LITERAL under elevation -- neither the CLI's native `env` expansion nor the loader's `args` substitution resolved a real value.

**Item 5b (Architect addition): with `export PROBE_VAR=<nonce>` appended to the target user's own login-shell profile, the SET fixture's spawn report carried the nonce in both `env` (CLI-native expansion) and `args` (the loader's substitution); with the export removed, both were literal again; the UNSET control was literal in all three runs.** This confirms `docs/design/embedded-agent-sdk-engine.md` §4.5's working assumption on both halves: the server process's own export does not cross the elevation boundary, and `${VAR}` does resolve against the TARGET USER's own login-shell profile once exported there. Mechanism: `spawnAsUser`'s elevated branch resets the login shell's environment and re-exports only the six `AGENT_CONSOLE_*` identity keys, so the server's export never crosses; the target user's own profile is read by the login shell it starts, same as that user's own interactive session. Three runs (no export / with export / export removed), clean separation, exit 0 all three times.

**A `ready`-before-MCP-connect race, found and fixed while building this arm.** The worker's `ready` event does not guarantee every declared project MCP server has finished connecting -- one local dry run (target user equal to the invoking user, no real elevation, free) observed `ready` fire, then a fixture's spawn report land roughly 1.5-3.2s LATER, with no prompt sent. `ready` (`sdk-engine.ts`) means the engine constructed the SDK query and started its consumer; it is NOT gated on the CLI's own `system:init`, which does not arrive until the first prompt is sent -- so `ready` fires essentially immediately, before the CLI's own project-server connections have necessarily made any progress. Reading the reports at the instant of `ready` can therefore observe an ABSENT report about to be written a moment later -- a false outcome (ii). The fix is a short GRACE WINDOW (currently 8s, capped by the same 60s overall gate deadline -- the gate's timeout is the bound, the grace window is not a second, unbounded wait) after `ready`/`fatal`/`turn-error` fires, during which the loop keeps polling the reports themselves and exits the instant both appear.

**A stale-image trap, found while running item 4.** `scripts/verify-multiuser-docker.sh --keep`, when a prior invocation's outer timeout kills the build mid-flight, leaves the PREVIOUS `agent-console-multiuser-verify:latest` image tag in place -- `docker compose up --build` only re-tags the image on a build that completes successfully, so a killed build silently runs OLD code with no build error of its own. The tell was a `Module not found` for a file the current worktree has but an image built days earlier does not. **Rebuild the image after any change to the probe or its fixture; a stale image runs stale code.**

Two properties of the script that matter when re-running it:

- **Non-elevated branch is billable and needs a real, authenticated `claude` CLI** for the invoking OS user -- the `claude-sdk` engine runs as the executing user and uses that user's own authentication, so there is no provider key to configure. One real turn (the model calls both fixture tools), well under $1. It also needs `git` on PATH for the throwaway repository's `.git`. It is a manual gate, never a CI job.
- **Elevated arm is free and needs elevation privilege for `<target-user>`** (a real OS user with a login shell and, per this section's own MUST NOT, no credential file of any kind seeded into its home beyond a credential-free `{}` `.claude.json`). `AUTH_MODE=multi-user` is forced on by the arm itself.
- **Exit codes**, same `PROBE_EXIT` convention as the sibling P-a/P-b file, for BOTH arms: `0` = MEASURED (every read observable completed and was read, regardless of what the values turned out to be -- a deviation from the anticipated shape is a finding to report, not a script failure); `1` = INCONCLUSIVE (non-elevated: a turn never settled, a tool call never happened, or its JSON payload could not be parsed; elevated: exactly one of the two spawn reports appeared, or the UNSET control fixture's reading disagreed with the non-elevated arm's own control shape); `2` = HARNESS (non-elevated: activation failed; elevated: outcome (ii), neither spawn report appeared within the gate's timeout -- the captured reason is printed as `ELEVATED branch: NOT REACHED (...)`).

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 2 (the elevated arm, turn-free, no login in the container) for the `--elevated` gate and readings when outcome (i) holds; Tier 4 (billable; dogfood host only) for the existing non-elevated branch and for a real-host, second-logged-in-user fallback if outcome (ii) holds.

## Additional Verification: Project MCP Server Permission Shipping-Path E2E

PRs touching `set_mcp_server_permission` in `packages/server/src/mcp/mcp-server.ts`, `resolvePermissionDecisions` / `listAllowedProjectMcpServerPairs` in `packages/server/src/lib/mcp-server-permissions.ts`, `SessionManager.setMcpServerPermissions`, `EmbeddedAgentWorkerService.applyMcpServerPermissions`, `SdkEngine.setMcpServers` / `classifyMcpServerScope`, or `packages/embedded-agent/src/mcp-discovery.ts`, must run `bun scripts/smoke/check-embedded-agent-project-mcp-permission.ts` locally before pushing. This is the shipping-path E2E for the `set_mcp_server_permission` MCP tool (epic [#1636](https://github.com/ms2sato/agent-console/issues/1636) Phase 5 PR-3a, Issue [#1795](https://github.com/ms2sato/agent-console/issues/1795)): it boots a disposable server instance with a real `AppContext` and a real `/mcp` on a real port, drives one real `claude-sdk` embedded-agent worktree worker against a real `.mcp.json` declaring two real stdio fixture MCP servers, and decides for it through the real tool using a bearer token minted for a real, non-embedded (TUI-Orchestrator-shaped) caller.

**Polarity FIRST, always.** `bun scripts/smoke/check-embedded-agent-project-mcp-permission.ts -- --expect-no-permission` activates the target worker and sends one turn with NO permission ever recorded, asserting neither fixture's spawn canary appears, both are discovered `pending`, and no tool-call names either server -- proving "nothing starts without a record" before any positive arm is billed. Omitting the flag continues from the SAME activation into the positive arms: (1) a live add (`Query.setMcpServers`) of one fixture through the tool, confirmed by its canary and a turn that actually calls its tool; (2) a restart proving the OTHER, durable half -- the allowed fixture starts from the persisted record with no live-add needed; (3) editing the still-pending fixture's `.mcp.json` entry (a hash change) and allowing it by the NEW hash, proving hash-change resilience on the live-add path; and negative controls in the same run -- a deny's durable effect across a restart, an EMBEDDED caller's refusal with nothing persisted, and an isolated `CLAUDE_CONFIG_DIR` proving the operator's own `~/.claude.json` is never read (no `user`/`local`-scope row ever appears, `userLocalNamesUnavailable` is never set).

**Arm 4 (Issue #1786) extends the same script to the quick-session, path-keyed permission table.** After the arms/controls above, the script creates three additional quick sessions, each with its own `claude-sdk` worker: Dir A (the arm's subject) is allowed through the real tool, live-added, confirmed by a real tool-call, and its allow SURVIVES a restart -- the identical allow/live-add/restart-persistence shape as arms 1/2, but against `mcp_server_path_permissions` (migration v45, keyed by realpath'd `location_path` via `resolveMcpPermissionScope`) instead of the repository-keyed table. Two controls run in the same session: (arm4-a) Dir B, a second quick session at a DIFFERENT path with a byte-identical `.mcp.json` declaration for the same server (same command, same args, including the same `--canary` path, so its discovered hash is asserted equal to Dir A's rather than merely assumed) stays `pending` -- proving the permission key includes `location_path` and is not satisfiable by `(serverName, hash)` alone, which a hash mismatch would otherwise make this control pass vacuously; (arm4-b) Dir C, a quick session whose `locationPath` is a SYMLINK to Dir A, discovers the server as ALREADY allowed and live-adds it from the inherited record -- proving the scope key is realpath'd, not the literal `locationPath` string.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user -- the `claude-sdk` builtin runs as the executing user and uses that user's own authentication, so there is no API key to configure. About 10-11 real turns per full run (5-6 for arms 1-3 + control (a), plus 5 for arm 4 and its two controls). It is a manual gate, never a CI job.
- **Classifier unit test**: `scripts/smoke/__tests__/check-embedded-agent-project-mcp-permission.test.ts` covers its pure argument-parsing and event-classifier functions only, and never runs the billed measurement; `registry-reachability.test.ts` and `import-safety.test.ts` enforce the rest.
- **The run's worker NDJSON is captured before the disposable home is deleted**, same convention as `check-fatal-incarnation-replacement.ts`'s capture step: the script's `finally` block copies every `**/outputs/**/*` file out of the disposable `AGENT_CONSOLE_HOME` to `$HOME/.agent-console-smoke-captures/check-embedded-agent-project-mcp-permission/<disposable-home-basename>/` — printed to stdout as `worker NDJSON captured to: ...` when anything was captured — strictly before `rm -rf` removes that home. Born from diagnosing the arm-3 read-too-early bug (Issue #1795): the capture let the exact `mcp-servers-discovered` sequence across a restart be inspected after an aborted run, without a billed re-run. Not auto-pruned; the operator who runs the smoke is responsible for removing capture directories they no longer need.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: Per-User claude.ai Connectors Toggle Shipping-Path E2E

PRs touching `SdkEngineDeps.disableClaudeAiConnectors` / the `disableClaudeAiConnectors` settings composition in `packages/embedded-agent/src/sdk-engine.ts`, the `init.disableClaudeAiConnectors` threading in `packages/embedded-agent/src/main.ts`, `EmbeddedAgentWorkerServiceDeps.resolveDisableClaudeAiConnectors` / its composition in `packages/server/src/services/embedded-agent-worker-service.ts`, `resolveDisableClaudeAiConnectors` in `packages/server/src/services/resolve-disable-claude-ai-connectors.ts`, `UserRepository.getPreferences` / `.setPreferences` (either implementation), or the `GET /api/auth/me` / `PATCH /api/auth/me/preferences` routes in `packages/server/src/routes/auth.ts`, must run `bun scripts/smoke/check-claude-sdk-connectors-toggle.ts` locally before pushing. This is the shipping-path E2E for the per-user claude.ai connectors toggle (Issue #1836): it boots a disposable server instance with a real `AppContext` and a real `/api` on a real port, drives two real `claude-sdk` embedded-agent workers for the SAME user through real activations, and toggles the preference through a real `PATCH /api/auth/me/preferences` HTTP request against the real running server -- never a direct repository call.

**The mechanism has no runtime setter, and the script's two-worker shape exists because of that.** Unlike compaction's `auto` toggle, `disableClaudeAiConnectors` is read once at `SdkEngine` construction and never re-read; a preference change takes effect at a worker's NEXT activation, never live. Worker A (activated BEFORE the PATCH) is the positive-control baseline; a SEPARATE, fresh worker B (activated AFTER the PATCH, same user) is what proves the new preference actually took effect at its next activation -- reactivating A in place would not distinguish "the fix works" from "the SDK never re-reads settings on a restart, for either value". The observable in both cases is the worker's own persisted `mcp-servers-discovered` event(s), read off its NDJSON output file, never inferred from the model's prose.

**The positive control is a gate, not a formality.** If the invoking OS user's Claude Code account has no claude.ai connectors configured, worker A observes zero `scope: 'connector'` rows and the run reports INCONCLUSIVE (exit 1) rather than a false "pass" -- there would be nothing for the toggle to suppress, and every downstream assertion would be meaningless.

**`--expect-connectors-present` is a same-run control, not a break-the-fix polarity flag** (there is no single source-level revert for this mechanism the way other smokes' `--expect-*` flags have). It skips the PATCH entirely and drives a fresh control worker through the identical activate-and-turn sequence with the preference left at its default (connectors ON), asserting connectors are still observed. Run this before the default run: it confirms a freshly-created worker's own harness can observe connectors at all, so the default run's "worker B: zero connector rows" reading is attributable to the toggle rather than to some unrelated property of a fresh worker/session.

Two properties of the script that matter when re-running it:

- **It is billable and needs a real, authenticated `claude` CLI** for the invoking OS user -- the `claude-sdk` builtin runs as the executing user and uses that user's own authentication, so there is no API key to configure. Two real turns per default run (one per worker), one real turn for the `--expect-connectors-present` control run. It is a manual gate, never a CI job.
- **No idle-threshold or elevation parameter.** Unlike this file's eviction/elevation siblings, this mechanism is neither time-driven nor elevation-coupled, so there is nothing analogous to `--idle-ms` or `--elevated` to substitute.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Backfilled sections (Issue #1637)

**The fourteen sections below register the fifteen scripts that had no `check:` alias and no section before Issue #1637 — the old "alias + section" rule never caught them** (one section, "SDK H2 Transport-Settle Probe Pair", covers two sibling files). Each section's touch-trigger list (the "PRs touching ..." clause) is derived MECHANICALLY from that script's own `import` statements and spawn targets at backfill time — not authored from design knowledge of why the script was originally written, since these fifteen predate this delegate's involvement. Purpose and requirements are lifted from the script's own header comment; where a header is silent on a fact (e.g. billing cost), that is stated as "not stated in the script header" rather than guessed. A future author touching one of these scripts substantively should expand its section with first-hand detail, the way every pre-existing section above does.

## Additional Verification: Delegated-Session SSH_AUTH_SOCK Fallback Smoke

PRs touching `buildElevationArgs` / `shellEscape` in `packages/server/src/services/elevation-args.ts` (specifically the `sshAuthSockFallback` option) must run `bun scripts/smoke/check-delegated-ssh-auth-sock.ts <target-user>` locally before pushing. It imports the production `buildElevationArgs` helper directly and drives it against the real OS `sudo` binary and a real target user's login shell, asserting the conditional `SSH_AUTH_SOCK` export shell snippet actually evaluates and takes effect, and that the snippet is absent entirely when the fallback option is not passed (back-compat lock). Because the helper is the single source of truth for both production and this script, drift between what production does and what the smoke verifies is impossible by construction.

Requires elevation rights for `<target-user>` (a real OS user with a login shell); a `.1password/agent.sock` in that user's home exercises the socket-existence branch, otherwise the absent-socket branch is verified instead. It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 2 (the verification container's `--smokes` run).

## Additional Verification: Multi-User PTY Env Propagation Smoke

PRs touching `buildElevationArgs` in `packages/server/src/services/elevation-args.ts` (the general argv shape, not just the SSH_AUTH_SOCK option above) must run `bun scripts/smoke/check-multiuser-pty-env.ts <target-user>` locally before pushing. Same construction as the SSH_AUTH_SOCK smoke -- imports `buildElevationArgs` directly and runs the real `sudo` argv against a real target user -- but asserts the general env-propagation contract (PATH / HOME / USER / SHELL and the rest of the login shell's init) rather than one specific fallback. Per the script's own header, its "sync contract" is `NONE`: because both production and this smoke call the same helper, a new env export in production is covered automatically.

Requires sudo privilege for `<target-user>` (a real OS user with a login shell). It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 2 (the verification container's `--smokes` run).

## Additional Verification: Embedded-Agent `Bash` Tool Env Non-Leakage Smoke

PRs touching the embedded-agent `Bash` builtin tool's env handling (`packages/embedded-agent/src/tools/env-cleaner.ts`'s allowlist), the identity injection at the embedded spawn (`buildAgentConsoleEnv` in `packages/server/src/services/agent-console-env.ts`, the `baseEnv` / `env` pair `EmbeddedAgentWorkerService` hands to `spawnAsUser`, or `AGENT_CONSOLE_IDENTITY_ENV_KEYS` in `packages/shared/src/constants/agent-console-env.ts`), or `packages/server/src/app-context.ts`'s wiring of it under multi-user elevation, must run `bun scripts/smoke/check-embedded-agent-bash-env.ts <target-user>` locally before pushing (FF-1b, Issue #1043; identity injection, Issue [#1694](https://github.com/ms2sato/agent-console/issues/1694)). Unlike the elevation-argv smokes above, this one drives a REAL scripted turn through an actual `Bash` tool-call/result round trip against a real second OS user via a real `AppContext`, and asserts the tool's `env` output shows `USER`/`LOGNAME` equal to the target user (proof the tool ran as that user under real elevation, not the server-process user), that it carries EXACTLY this worker's own identity records -- `AGENT_CONSOLE_BASE_URL` / `SESSION_ID` / `WORKER_ID` with the activated session's values, line-anchored, and no other `AGENT_CONSOLE_*` key (the #1694 acceptance sentence; the pre-#1694 shape asserted no `AGENT_CONSOLE_*` key at all, which is exactly what the injection makes false) -- and that the provider key does not leak into it. Polarity was measured in the tier-2 container when #1694 landed: the flipped assertion fails on the pre-fix tree and passes with the fix.

The disposable `AGENT_CONSOLE_HOME` this smoke builds emulates the production data-root's `2775` setgid contract (Issue #1713) via the shared `disposable-multi-user-home.ts` helper, so the memory layer's verification (`memory-dir.ts`) does not fail closed against a plain, non-setgid `mkdir -p` home under `AUTH_MODE=multi-user`.

Requires elevation privilege for `<target-user>` and `bun install` already run (so `@agent-console/embedded-agent` resolves in the server package's workspace). It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 2 (the verification container's `--smokes` run).

## Additional Verification: Embedded-Agent Worker Elevation (Phase 4) Smoke

PRs touching `SessionManager.activateEmbeddedAgentWorker`'s elevated-spawn path, `resolveEmbeddedAgentEntryPath()`, the `AGENT_CONSOLE_MCP_AUTH` resolution (`resolveMcpAuthMode`; the smoke sets `enforce` explicitly, since the multi-user default is `warn` until Issue [#1107](https://github.com/ms2sato/agent-console/issues/1107) -- an earlier revision wrongly relied on an unset value resolving to `enforce`, Issue [#1738](https://github.com/ms2sato/agent-console/issues/1738)), or the identity env the embedded spawn injects (`buildAgentConsoleEnv` / `spawnAsUser`'s `env` on the elevated branch), must run `bun scripts/smoke/check-embedded-agent-elevation.ts <target-user>` locally before pushing. Since Issue [#1694](https://github.com/ms2sato/agent-console/issues/1694) it also carries a POSITIVE identity assertion in the same run as its negative secret checks: scanning `/proc/*/environ` AS THE TARGET USER (through the real `runAsUser`, because another user's `environ` is EACCES for the server process) with the orphan sweep's exact match semantics (`grep -Fxz`), at least one live process must carry the record `AGENT_CONSOLE_SESSION_ID=<activated sessionId>`, while a never-activated id must match zero processes (the in-run negative control). That is also the proof that the embedded loop tree is now in the sweep's population (C6). Polarity measured in the tier-2 container when #1694 landed: fails on the pre-fix tree (`matches=0`), passes with the fix. This is the smoke bullet referenced by `docs/design/embedded-agent-worker.md` Part II's Testing plan: it drives the REAL shipping path -- `SessionManager.activateEmbeddedAgentWorker` spawning the real embedded-agent loop subprocess via the real production `spawnAsUser` -- against a real second OS user, with `AUTH_MODE=multi-user` forced on and `AGENT_CONSOLE_MCP_AUTH` set explicitly by its `--auth-mode enforce|warn` flag, default `enforce` (no text in this repository should claim an unset value resolves to `enforce` -- it resolves to `warn`). **It carries two arms, and both must be run (Issue [#1738](https://github.com/ms2sato/agent-console/issues/1738)).** After `ready` and before teardown, against the real Hono app on its real port, the smoke sends the same JSON-RPC `tools/call` of `list_sessions` twice: E1 tokenless, E2 with the bearer token the loop itself presented (captured from the real `/mcp` request the init handshake made). The default arm asserts E1 is refused with HTTP 401 and the gate's exact enforce message, that the gate's warn line was NOT logged, and that E2 is admitted (200, a result) -- the token is what admits the call, not merely something that reached `/mcp`. `--auth-mode warn` asserts the inverse of E1 on the same call (accepted, 200, and the gate's exact warn line logged exactly once, observed through a recording pass-through on `rootLogger.warn`) with E2 still admitted. Running either arm against the other's assertion set fails, which is the polarity; the tier-2 driver runs both arms, and the effective mode is read back from the running instance by that observable behaviour (401 vs 200), not by a mode-echo endpoint.

The disposable `AGENT_CONSOLE_HOME` this smoke builds emulates the production data-root's `2775` setgid contract (Issue #1713) via the shared `disposable-multi-user-home.ts` helper, so the memory layer's verification (`memory-dir.ts`) does not fail closed against a plain, non-setgid `mkdir -p` home under `AUTH_MODE=multi-user`.

Passing the current process user as `<target-user>` exercises the entire pipeline except the actual cross-user `sudo` boundary crossing (a degraded but still useful mode when no second OS user is configured). Requires elevation privilege for `<target-user>` and `bun install` already run. It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 2 (the verification container's `--smokes` run, which invokes BOTH arms) and Tier 3 (systemd stack, against the real MainPID) — `scripts/verify-multiuser-systemd.sh` step 9 runs this exact smoke's default (`enforce`) arm against the real unit's `MainPID`.

## Additional Verification: `killAsUser` Real-Elevation Smoke

PRs touching `spawnAsUser` / `killAsUser` in `packages/server/src/services/privilege-elevation.ts` must run `bun scripts/smoke/check-kill-as-user.ts <target-user>` locally before pushing (Issue #1197 Part A). It drives the real production helpers against a real long-lived `sleep` process spawned as `<target-user>`, asserts a real elevated `SIGTERM` via `killAsUser` actually terminates the targeted PID, and asserts a negative case: a second, unrelated `sleep` process survives the call, proving the helper signals exactly the targeted PID rather than something broader like a name-based `pkill`.

Requires elevation privilege for `<target-user>`; passing the current process user as `<target-user>` runs in a degraded mode that skips the real `sudo` boundary. It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 2 (the verification container's `--smokes` run).

## Additional Verification: Orphan-Process Sweep Real-Elevation Smoke

PRs touching `spawnAsUser` in `packages/server/src/services/privilege-elevation.ts` or `sweepOrphanProcesses` in `packages/server/src/services/orphan-process-sweeper.ts` must run `bun scripts/smoke/check-orphan-sweep.ts <target-user>` locally before pushing (Issue #1197 Part B). It drives the real production helpers against real long-lived processes spawned as `<target-user>` with an `AGENT_CONSOLE_SESSION_ID` marker, and asserts the real elevated sweep command actually reaps them by that marker.

Requires elevation privilege for `<target-user>`; passing the current process user as `<target-user>` runs in a degraded mode that skips the real `sudo` boundary. It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 2 (the verification container's `--smokes` run).

## Additional Verification: Login-Shell Sentinel Protocol Smoke

PRs touching the login-shell sentinel PTY protocol -- `buildDirectSentinelShellCommand` / `buildElevatedSentinelCommand` (`packages/server/src/services/sentinel-spawn-command.ts`), `bunPtyProvider`, or `getUnsetEnvPrefix` in `packages/server/src/services/env-filter.ts` -- must run `bun scripts/smoke/check-login-shell-sentinel.ts` (direct mode) and, when elevation is available, `bun scripts/smoke/check-login-shell-sentinel.ts --elevated <target-user>` locally before pushing. It spawns a REAL PTY running a real login shell via the same production `bunPtyProvider` and command builders that `MultiUserMode` / `SingleUserMode` use, waits for the sentinel line, injects a probe command, and asserts the observable end state.

Direct mode requires only a login shell resolvable via `$SHELL` for the current user; `--elevated` mode requires elevation privilege for `<target-user>`. It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 2 (the verification container's `--smokes` run).

## Additional Verification: Exit-127 Spawn-Failure Diagnostic Smoke

PRs touching `appendSpawnFailureNotification`, `WorkerManager.activateAgentWorkerPty` / `setupWorkerEventHandlers`'s `onExit` handler, or `SingleUserMode.spawnPty`'s command construction, must run `bun scripts/smoke/check-exit-127-diagnostic.ts` locally before pushing (Issue #1294). It drives the REAL production chain end-to-end -- `bunPtyProvider` -> `SingleUserMode.spawnPty` -> `WorkerManager`'s `onExit` handler -> `appendSpawnFailureNotification` -> `WorkerOutputFileManager` (real fs, no memfs) -- against a real process whose `$SHELL` points at a nonexistent binary, reproducing the exact exit-127/zero-PTY-bytes/pre-sentinel signature the Issue was diagnosed from, and asserts the diagnostic message reaches disk even with zero WebSocket clients attached, and that `worker.pty` only transitions to `null` after that diagnostic append+flush completes.

Free and deterministic -- no elevation, no billed CLI. It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Unprivileged (no tier-2/3/4 residue; any machine with bun).

## Additional Verification: PTY + AsyncLocalStorage Data-Delivery Regression Smoke

Before any Bun runtime upgrade or floor change to `MIN_BUN_VERSION` in `scripts/check-bun-version.mjs`, or when touching `bunTerminalProvider` / `BunTerminalPtyAdapter` in `packages/server/src/lib/pty-provider.ts`, run `bun scripts/smoke/check-pty-als-data.ts` locally. On Bun 1.3.5-1.3.13, `Bun.spawn({ terminal })`'s `terminal.data` callback never fires when the spawn happens inside an active `AsyncLocalStorage` context (e.g. the MCP request scope agent-worker PTYs are created under) -- zero bytes ever reach JS. This smoke spawns real PTYs inside an active ALS scope via the real `bunTerminalProvider` and asserts the completion marker is observed on every cycle; it is the designated canary this repo's own `MIN_BUN_VERSION` floor comment points at.

Free and deterministic (no elevation, no billed CLI, ~15 seconds). It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Unprivileged (no tier-2/3/4 residue; any machine with bun).

## Additional Verification: PTY Pre-Attach Output Buffer Smoke

PRs touching `bunTerminalProvider` / `BunTerminalPtyAdapter`'s pre-attach data buffering in `packages/server/src/lib/pty-provider.ts` must run `bun scripts/smoke/check-pty-early-output.ts` locally before pushing (Issue #1242). Two silent byte-loss windows existed before the pre-attach buffer fix -- between `Bun.spawn()` returning and adapter construction, and between adapter construction and the first `onData()` attach -- and this smoke drives real spawn cycles through the real provider to assert no early bytes are lost in either window; the sibling unit test (`pty-provider.test.ts`) proves the buffer/flush logic against a mocked `Bun.spawn`, which is necessary but not sufficient on its own.

Free and deterministic (no elevation, no billed CLI). It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Unprivileged (no tier-2/3/4 residue; any machine with bun).

## Additional Verification: Stdin-Sink FD Leak Smoke

PRs touching `InteractiveProcessManager`'s or `EmbeddedAgentWorkerService`'s stdin-feeding teardown (the `endStdinSafely` helper each maintains per `.claude/rules/elevation-helpers.md`'s "feeding-consumer teardown obligation"), or `spawnAsUser` in `packages/server/src/services/privilege-elevation.ts`, must run `bun scripts/smoke/check-stdin-sink-leak.ts` locally before pushing (Issue #1230). It imports the production `InteractiveProcessManager` and `spawnAsUser` directly and drives real long-lived `spawnAsUser` consumers through their stdin lifecycle, asserting the underlying `FileSink`'s write-end fd is released deterministically at teardown rather than left for incidental GC -- the same unsound-pattern concern `check-pty-fd-leak.ts` verifies for the PTY master-fd handle.

Linux-only (reads `/proc` directly); free and deterministic otherwise. It is a manual gate, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Unprivileged (no tier-2/3/4 residue; any machine with bun) — the Linux-only note above is a `/proc` dependency, not an elevation tier.

## Additional Verification: SDK Compaction Premises Probe

Before changing any of the compaction-related surface named in `docs/design/embedded-agent-sdk-engine.md` §5's PS1/PS2 (`autoCompactEnabled` / `autoCompactThreshold` / `autoCompactWindow` settings, the `/compact` slash command, or `PreCompact`/`PostCompact` hooks), run `bun scripts/smoke/probe-sdk-compaction.ts` against a real, authenticated `claude` CLI session (Issue #1400). Measurement only, not a pass/fail gate: item flags (`--p1a`, `--p1b`, `--p3i`, `--p2`, `--p3-on`, `--p3-neg`, `--p4-hooks`; default is the cheap `--p1a --p3i --p2` set) each settle a distinct premise about whether a setting arrives, is writable, or actually changes behavior, and every negative conclusion is paired with a same-run positive control (see the script's own header for the full CTRL-* list). Shares its live-session harness with `probe-sdk-resume.ts` via `probe-sdk-session-harness.ts`.

Requires a real, authenticated `claude` CLI session and `bun install` already run. It is a manual re-verification tool, run by hand on SDK bumps, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: SDK Resume (PS4) Premises Probe

Before relying on any claim about whether `Options.resume` restores a session's conversation across a process kill, run `bun scripts/smoke/probe-sdk-resume.ts` against a real, authenticated `claude` CLI session (docs/design/embedded-agent-sdk-engine.md §5's PS4; Issue #1400). Item flags (`--basic`, default; `--invalid`; `--post-compact`) cover the idle-kill-then-resume pass condition, the negative "resume an unknown session id" case (R1/#1410's PS6/PS7 premises), and the eviction-x-compaction composite, each with same-run positive controls (a pre-kill recall, a post-resume ordinary turn, and a fresh-session-without-resume negative control -- see the script's own header for the full CTRL-* list). Shares its live-session harness with `probe-sdk-compaction.ts` via `probe-sdk-session-harness.ts`.

Requires a real, authenticated `claude` CLI session and `bun install` already run. It is a manual re-verification tool, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Additional Verification: SDK H2 Transport-Settle Probe Pair

Before relying on any claim about whether calling `Query.getContextUsage()` immediately after a turn's `result` can throw "ProcessTransport is not ready for writing" (docs/design/embedded-agent-sdk-engine.md §5's H2), run `bun scripts/smoke/probe-sdk-h2-transport-settle.ts [trials] [--with-tool]` against a real, authenticated `claude` CLI session. This is the PRODUCTION-FAITHFUL half of a pair: it keeps its `for await` loop over the `Query`'s message stream running for the entire probe and calls `getContextUsage()` from inside that still-live loop body, exactly matching `sdk-engine.ts`'s own `consumeLoop` chain.

**Read together with its sibling, `bun scripts/smoke/probe-sdk-h2-transport-settle-negative-control.ts [trials]`, which deliberately uses a WRONG methodology** (breaking the `for await` loop on the `result` message before calling `getContextUsage()`) that reliably reproduces a false-positive of the same error on every SDK version tested, regardless of whether the real race is present -- existing specifically so a future re-prober who reaches for the "obvious" break-on-result pattern sees it fail first and understands why before drawing any conclusion about the SDK's real transport behavior. The negative control's expected, correct outcome is every trial reproducing the artifact; a clean run there means the artifact stopped reproducing and is itself worth investigating, not that the SDK improved.

Both require a real, authenticated `claude` CLI session and `bun install` already run so `@anthropic-ai/claude-agent-sdk` resolves to the version under test. Both are manual re-verification tools, run by hand on SDK bumps, never a CI job.

**Verification tier (Discipline 4 of `os-environment-coupling.md`):** Tier 4 (billable; dogfood host only -- permanent residue).

## Exceptions to the reachability rule

**A `scripts/smoke/*` file with no top-level `import.meta.main` guard is a shared library, not an entry point, and needs no section of its own.** The guard (`if (import.meta.main) { main(); }`, required of every entry point per "A new smoke script is born import-safe and born-covered" above) is what makes a file directly runnable; its absence is exactly what makes a file a library instead -- nobody invokes it with `bun scripts/smoke/<file>`, so the reachability rule's rationale ("a script nobody can find is a script nobody re-runs") does not apply to it. One file this currently exempts is `probe-sdk-session-harness.ts`, whose own header says so explicitly ("Not an entry point -- it has no `main()` and running it directly does nothing"): it is a shared live-session harness imported by `probe-sdk-compaction.ts` and `probe-sdk-resume.ts` (both documented above, both crediting it as their shared methodology), reachable through those two consumers instead. A second is `disposable-multi-user-home.ts` (Issue #1713), a shared helper for building a disposable `AGENT_CONSOLE_HOME` that satisfies the production data-root's `2775` setgid contract -- reachable through its two consumers, `check-embedded-agent-bash-env.ts` and `check-embedded-agent-elevation.ts` (both documented above). `scripts/smoke/__tests__/registry-reachability.test.ts` derives this exemption from the same content-based rule (guard absence), not from a hand-maintained name list, so a future library file is exempted automatically and a future genuinely-unregistered script (which necessarily has the guard) cannot slip into this bucket.

## Before Creating a PR

Run the coverage check to verify all production files have corresponding tests:

```bash
# With a PR number (uses gh pr diff):
node .claude/skills/orchestrator/preflight-check.js <PR-number>

# Without a PR number (uses local git diff against origin/main):
node .claude/skills/orchestrator/preflight-check.js
```

If any gaps are detected (non-zero exit code), add the missing tests before proceeding.

## Mirror Maintenance

The patterns above are a **markdown mirror** of the executable single-writer `COVERAGE_PATTERNS` in `.claude/skills/orchestrator/check-utils.js` (the source of truth used by `preflight-check.js`). When updating one, update the other in the same PR.

Drift between the two is detected mechanically by `.claude/skills/orchestrator/check-mirror-drift.js` (CI workflow: `.github/workflows/check-mirror-drift.yml`). To verify locally:

```bash
node .claude/skills/orchestrator/check-mirror-drift.js
```

The check normalizes the regex `^DIR\/.+\.EXT$` shape to its glob `DIR/**/*.EXT` equivalent and compares against both the markdown table above and the YAML `globs:` frontmatter. Negation entries in the YAML (`!**/*.test.ts`, `!**/__tests__/**`) mirror the `isTestFile()` helper rather than `COVERAGE_PATTERNS`, and are excluded from the comparison. (Issue #752)
